# Task Tracker — Shore-Diving Intelligence & Discovery Platform

Decomposed, checkable breakdown of `plan.md` (Hardened Spec v4) for progress tracking. This file is the "how, in granular steps"; `plan.md` is the "what and why" — each item below traces back to a task/decision there so nobody has to re-derive the reasoning.

**Status markers:** `[ ]` not started · `[x]` done · `[~]` in progress · `[!]` blocked (reason noted inline).

**Convention, added 2026-08-08 after a five-lens adversarial review of `plan.md` found this being applied inconsistently:** `[x]` requires the feature to be reachable via a real navigation path in the live app — a disclosed demo route (`/post-dive`, `/media-demo`) is `[~]` at best; a component that's built but mounted by zero routes (no demo, no real page — worse than either) is `[~]` or `[ ]`, never `[x]`, regardless of how complete the underlying code is. See `plan.md` v5's Engineering Standards addenda for the incident that prompted this.

**Traceability convention:** `(→ Task N)` / `(→ P0-X)` points to `plan.md`; `(→ §N)` points to a `THREAT_MODEL.md` finding. IDs (e.g. `P0-A.2`, `T15.4`) are stable — reference them in commits/PRs.

Update checkboxes as work lands. Don't let this drift from reality — if a task's scope changes, update `plan.md` first, then reflect it here.

---

## Phase -1: Scaffolding — done this session

- [x] `S.1` Next.js (App Router) + TypeScript + Tailwind scaffolded, builds and lints clean
- [x] `S.2` Serwist PWA layer configured (service worker + `manifest.ts`); `dev`/`build` forced to webpack (Turbopack unsupported by Serwist)
- [x] `S.3` Supabase client helper created (`src/lib/supabase.ts`) — not yet pointed at a real project
- [x] `S.4` Mapbox map component created (`src/components/site-map.tsx`) — degrades gracefully without a token, not yet pointed at a real token
- [x] `S.5` Stray home-directory npm/node_modules cruft removed (was corrupting installs)

## Phase 0: Foundational Prerequisites (→ blocks Phase 1)

### P0-A: Identity & Auth (→ P0-A)
- [x] `P0-A.1` Create Supabase project; add URL/anon key to `.env.local` and Vercel env vars *(founder action — external account)* — **fully done and verified live, 2026-08-08.** `.env.local` has real `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and now `SUPABASE_SERVICE_ROLE_KEY` too. All 6 migrations (`0001`–`0006`) applied via the SQL editor, RLS enabled during `0001` per the dashboard's own warning (correct call — a brief deny-all window until `0002`'s policies land, not a security gap). Verified end-to-end, not just "tables exist": anon reads on `profiles`/`camera_sources` correctly return `[]` (row-scoped by RLS), an anon `INSERT` into `sites` is correctly rejected (`42501`, RLS policy violation), the `offline-bundles` Storage bucket exists with the exact config `0004` specifies (20MiB limit, correct MIME allowlist), and a full `npm run build` + live `npm run dev` smoke test confirms `/`, `/login`, `/moderation/camera-sources` (correctly shows "Sign-in required" now instead of "Supabase not configured") and the `CRON_SECRET`-gated admin route (correctly fails closed, 401, since `CRON_SECRET` is still unset) all behave exactly as designed against the real project. Vercel env vars remain unconfirmed (local-only check, not applicable until a deploy happens).
- [x] `P0-A.2` Configure Google OAuth provider in Supabase Auth (free — Google Cloud OAuth client) — confirmed live 2026-08-08, verified two ways rather than taken on faith: the Auth service's public settings endpoint (`/auth/v1/settings`) reports `"google": true`, and hitting the real `/auth/v1/authorize?provider=google` endpoint returns a genuine `302` to Google's actual consent screen with a valid `client_id` — proving the OAuth client is functionally wired, not just toggled on in the UI with a blank credential. Full sign-in still needs a real human clicking through Google's consent screen to fully prove out (not something I can do headlessly), but everything up to that point is confirmed working.
- ~~`P0-A.3` Configure Apple Sign In provider~~ — **dropped for v1** (founder decision): requires a paid Apple Developer Program membership ($99/yr) with no free path, conflicts with no-budget context. **v1 is Google-only.** Revisit if there's budget or real demand later.
- [x] `P0-A.4` Build sign-in UI + OAuth callback route (Google only) — `src/app/login/page.tsx`, `src/app/auth/callback/route.ts`. Confirmed working end-to-end live 2026-08-08 (real Google OAuth redirect, real `client_id`) — one real limitation found along the way: Google blocks any CDP-automated browser (e.g. `agent-browser`, even in headed mode) from completing its consent screen ("This browser or app may not be secure") — not a bug here, a deliberate Google anti-phishing policy; a real OAuth login always needs a human in a normal, non-automated browser. **Bug found 2026-08-08 by the adversarial UX review, fixed same day:** `auth/callback/route.ts` redirects to `/login?error=...` with a real, specific message on three failure paths (missing code, failed session exchange, thrown exception), but `login/page.tsx` didn't read it — silently dropped, bare button, no explanation. **Fixed:** a small `OAuthCallbackError` client component reads `?error=` via `useSearchParams()` (Suspense-wrapped — confirmed against this Next.js version's docs that a statically-rendered route needs this, verified by actually running `npm run build`), feeds the existing shared error-banner state, then strips the param via `router.replace()` so a refresh doesn't re-show a stale error. Verified independently: `tsc`/lint/tests/build all clean, and a live dev-server check confirmed `/login?error=test%20message` renders the banner.
- [x] `P0-A.5` Session handling / auth guard for protected routes — `src/proxy.ts` handles session-cookie refresh (the "session handling" half). **Found and fixed 2026-08-08: the "auth guard for protected routes" half was never actually built** — `proxy.ts` has zero route-protection logic (no redirect, no protected-route list), and nothing in the UI linked to `/login` at all, so the page worked but was undiscoverable. This was a genuine oversight, not a deliberate scope cut — the task's own title promised route protection that was never delivered, and it got marked done anyway. Fixed with `src/components/site-header/` (a global header wired into `src/app/layout.tsx`, checking real auth state server-side and showing "Sign in" or the user's email + a new `SignOutButton` — sign-out didn't exist anywhere either) rather than a blanket proxy-level redirect: most of this app is intentionally public (map/hazard/LDS data per `0002_rls.sql`'s RLS, Safe-Return is local-only), and the two routes that actually need gating (`/moderation/camera-sources`, `/webcam-discovery`) already do their own inline auth check and render a real sign-in prompt — matching that established pattern beat adding a redundant global redirect. Side effect worth knowing: every route is now dynamically rendered (`export const dynamic = "force-dynamic"` in `layout.tsx`) instead of statically generated, since the header needs live cookies on every request — correct tradeoff, but a real one.
- [x] `P0-A.6` Create `profiles` table, populated on first sign-in — schema/trigger side in `supabase/migrations/0001_init.sql` (`handle_new_user()`), app-code side in `src/lib/supabase/profile.ts`'s `ensureProfile()` (called from the callback route, swallows errors gracefully since the table doesn't exist in any live project yet). **Note:** these two mechanisms are redundant (both idempotent, both safe to keep) — the DB trigger is the authoritative path; `ensureProfile()` is a harmless no-op once it runs. Fine as belt-and-suspenders, revisit only if it becomes confusing.

### P0-B: Data Provenance Model (→ P0-B, → §1, → §8)
- [x] `P0-B.1` Design `sites` schema: coordinates, description, `VERIFIED`/`COMMUNITY` tag, legal/access-status field, created_by, timestamps — `supabase/migrations/0001_init.sql`. **Gap closed 2026-08-08 by `T11.5`:** `src/lib/sites/queries.ts` is now real application code querying this table (`listSitesWithHazardFlag`, `getSiteWithHazards`), wired into both the homepage map/list and the new site detail page. See `T11.5` below for the full verification record.
- [x] `P0-B.2` Design `hazard_reports` schema with provenance tag + timestamp — same migration
- [x] `P0-B.3` Design `lds_status` (fill station) schema with provenance tag + last-verified timestamp — same migration; modeled as an append-only log (mirrors `hazard_reports`), not update-in-place — confirm this fits when Task 16 is built
- [x] `P0-B.4` Row-level security: public read, authenticated write, rate-limited community submissions — `supabase/migrations/0002_rls.sql`; validated end-to-end against a scratch local Postgres (anon read-only, community users can't self-escalate to `VERIFIED`, can't edit others' rows). **Rate limiting is now implemented** — `supabase/migrations/0006_rate_limiting.sql`: a reusable `before insert` Postgres trigger (`enforce_submission_rate_limit()`, not `security definer`, runs in the normal RLS-enforced path) capping `hazard_reports`/`lds_status`/`camera_sources` at 5 inserts per user per 10 minutes (`camera_sources` included as a judgment call beyond `0002`'s literal TODO — same self-service-insert-capped-at-non-privileged-status shape and abuse exposure). Chosen over an application-layer check because this app's write path is the Supabase client SDK called directly from the browser, with no server route to hang an app-layer check on. Verified end-to-end against a throwaway Postgres container: 5 inserts succeed, the 6th is rejected, on all three tables; `sites` (deliberately out of scope) unaffected.
- [x] `P0-B.5` Provenance-tier visual styling (map pins, status badges) — `src/components/provenance-badge.tsx`; also supports `MODEL_INFERRED` (styled now, unused until Task 18)
- [ ] `P0-B.6` Seed initial site data (founder-curated — content task, not engineering, since there's no community yet) — starter example in `supabase/seed.sql` (3 real sites incl. La Jolla Cove), real curation still a founder task

### P0-C: PII Handling (→ P0-C, → §11)
- [x] `P0-C.1` Verify Supabase encryption-at-rest defaults and enforce HTTPS/TLS everywhere — TLS confirmed directly (2026-08-08): `NEXT_PUBLIC_SUPABASE_URL` is `https://` and every REST/Storage call this session succeeded only over TLS, with no plaintext fallback offered by the platform. Encryption-at-rest is a Supabase-managed-Postgres platform default (AES-256), not independently verifiable via the REST API — taking Supabase's own documented guarantee at face value here is the intended scope of this checklist item, not a gap.
- [x] `P0-C.2` Write a minimal data-retention policy (what gets deleted, when) and implement it — documented in `supabase/README.md`
- [x] `P0-C.3` RLS confirms profile/location data is readable only by its owner — `supabase/migrations/0002_rls.sql`'s `profiles_select_own`/`profiles_update_own` policies (`auth.uid() = id`) already satisfy this for everything that currently exists server-side. **`dive_plans` now exists** (`0007_dive_plans.sql`, `T11.5`, not yet applied to the live project) and follows the same owner-only shape — every policy is `auth.uid() = user_id`, no public-read policy, no `anon` grant. The Safe-Return timer's state remains localStorage-only (`T15.7`), never sent to Supabase.

## Phase 1: Core Telemetry & Offline Resilience

### Task 11.5: Dive Plan creation & Site Detail (→ plan.md v5 Priority Triage #1/#3/#5, new — blocks real completion of 12/14/16)
- [x] `T11.5.1` Real `sites`/`hazard_reports` query layer — `src/lib/sites/queries.ts` (`listSitesWithHazardFlag`, `getSiteWithHazards`), `src/lib/sites/types.ts`, `src/lib/sites/logger.ts`. Every function wraps Supabase calls in try/catch, returns an explicit `error` field, never throws (CLAUDE.md's I/O-boundary standard). Coordinates coerced `string → number` (`toNumber`) since PostgREST can serialize Postgres `numeric` as a JSON string.
- [x] `T11.5.2` Real map pins wired into `src/components/site-map.tsx` (`siteMarkers` prop, defaults to `[]`) and `src/app/page.tsx` (real server-side fetch + a plain "Dive sites" list fallback that works with or without a Mapbox token). **Map-pin legibility fix** (same-day aesthetic review finding, "doesn't pass a glance test" — two independent lenses converged on it): the provenance ring is no longer hue-coded; it's a single high-contrast near-white (`#fafafa`) stroke, VERIFIED (solid) vs. COMMUNITY (dashed) carried by stroke pattern, reusing `ProvenanceBadge`'s existing dashed = MODEL_INFERRED/unconfirmed grammar rather than inventing a new one — also resolves a separately-flagged colorblind-legibility concern, since pattern doesn't depend on hue discrimination. Legal-access status renders as a small corner glyph, shown only for the 4 restricted tiers (open/unassessed get no glyph, the documented low-noise default).
- [x] `T11.5.3` Site detail route — `src/app/sites/[id]/page.tsx`. Public read (no auth wall on site/hazard data, matching `sites`' RLS), server-side `getUser()` check only gating the "add to dive plan" section; a failed auth check degrades to a sign-in prompt, never blocks the public data. Real not-found and query-error states (no crash on a bogus ID).
- [x] `T11.5.4` `LegalAccessBadge` — `src/components/legal-access-badge.tsx`. Renders all 5 states of `legal_access_status` including `null` ("not yet assessed," reuses the dashed-border grammar), built icon-for-icon from the approved `creative/mockups/map-exploration/07-legal-access-badge-reference.html` reference. Icons hand-inlined rather than adding `lucide-react` as a new dependency — not this task's call per CLAUDE.md's "no paid [or new] dependency without checking" standard, even though the package itself is free.
- [x] `T11.5.5` Minimum-viable `dive_plans` table — `supabase/migrations/0007_dive_plans.sql` (Resolved Decision 5's exact field list: site, planned date/window, optional "diving with" label, status). Owner-only RLS (`auth.uid() = user_id` on every operation, including delete — no moderation dimension on private data), `site_id` nullable (single-site MVP compromise vs. the plural "site(s)" spec, documented in the migration header), `user_id` cascades on delete (unlike community tables' `set null` — an orphaned private plan isn't a meaningful row). **Not yet applied to the live Supabase project** — same manual SQL-editor process as `0001`–`0006` (`supabase/README.md` "How to apply"); founder action still open.
- [x] `T11.5.6` "Add to dive plan" action — `src/app/sites/[id]/add-to-dive-plan-form.tsx` (client form) + `src/app/api/dive-plans/route.ts` (API route, `user_id` always taken from the authenticated session, never the request body). Follows the same `NextRequest`/`NextResponse` + try/catch + structured-logger convention as `src/app/api/camera-sources/[id]/review/route.ts`.
- [x] `T11.5.7` Wires the already-built-but-unreachable `PrefetchButton`/`PretripChecklist` (Task 12) into the site detail page — closes `T12.6`/`T12.7`'s "mounted by zero routes" downgrade.
- [x] `T11.5.8` **Location-aware map + nearby-sites list**, added 2026-08-08 after the founder noticed the homepage always centered on California regardless of where they actually were. Root cause was two-fold: (1) `site-map.tsx`'s `DEFAULT_VIEW` was a hardcoded La Jolla coordinate with no geolocation lookup at all, and (2) the "Dive sites" list showed every seeded site with no location awareness. Fixed both: `src/hooks/use-geolocation.ts` (new, shared one-shot browser-geolocation hook, wraps the Geolocation browser-API boundary per CLAUDE.md's standard), `src/components/site-map.tsx` (now flies to the diver's real location once resolved, falls back to the CA coordinate silently if denied/unsupported), `src/lib/sites/distance.ts` (new, pure Haversine distance function, unit-tested), `src/components/nearby-dive-sites-list.tsx` (new — replaces the old unfiltered list; radius selector defaults to 25mi per the founder's explicit ask, expandable to 50/100/250/All, sorted nearest-first, falls back to the full unfiltered list rather than hiding everything while location is pending/denied). **A real hydration bug was caught and fixed during live verification**: an earlier version of `use-geolocation.ts` computed its initial state by branching on `typeof navigator`, which is undefined during SSR — the server-rendered HTML and the client's first paint disagreed, a genuine React hydration-mismatch error (caught via `agent-browser`'s dev-mode issues overlay, not by tsc/lint/tests, none of which catch this class of bug). Fixed per the codebase's own established pattern for this exact problem (`use-post-dive-prompt-settings.ts`'s doc comment on why a naive effect-based hydration flag is wrong): initial state must be identical on server and client, environment-dependent resolution only happens post-hydration inside the effect. Verified live end-to-end: `tsc`/lint/109-test-suite (4 new distance tests)/build all clean; `agent-browser` with a stubbed Fort Lauderdale, FL coordinate confirmed the map recenters correctly, the list correctly shows "No dive sites within 25 mi of your location yet" (honest — there are genuinely zero non-California sites yet, see `T11.5.9`), and expanding the radius to "All" correctly lists all 3 seeded CA sites sorted by real distance (2269–2331 mi) with per-site mile labels. A fresh full-page reload after the fix showed zero hydration errors (confirmed via the dev issues overlay, not just absence-of-crash).
- `T11.5.9` **Real dive-site content beyond the 3 CA seed rows — surfaced, not yet actioned.** Investigating the Florida-defaulting question surfaced a bigger gap: there is currently no way for *anyone* — founder or community — to add a `sites` row through the app (unlike `hazard_reports`/`lds_status`, which both already have real submission UIs; `sites` has zero insert path anywhere in the codebase). Two research passes (2026-08-08, founder-directed, no code/DB changes made) are recorded for a future session: candidate real South Florida shore-dive sites with sourcing, and an external/licensed dive-site data source landscape survey (OpenStreetMap/Overpass has real but patchy shore-dive coverage under ODbL, NOAA is clean CC0 for the legal/hazard layer, PADI has no public API, FWC's artificial-reef dataset's license is unconfirmed). Neither research output has been inserted into the database or acted on — pending founder review.

**Verified 2026-08-08**, independently of the implementing agent's self-report (same rigor as every other change this session): read every new/changed file; `npx tsc --noEmit`, `npm run lint`, `npm run test` (105/105), and `npm run build` all run clean from a fresh shell; live `agent-browser` check against the real seeded Supabase project confirmed the homepage renders all 3 real sites (map pins + list, both), a bogus site ID renders a real "Site not found" state (not a crash), an unauthenticated `POST /api/dive-plans` correctly 401s, and a zoomed/cropped screenshot of the map pins confirms the legibility fix actually renders (solid near-white stroke, amber corner glyph on the MPA-tagged site) — not just claimed in the diff.

**Known follow-ups, explicitly scoped out of this pass, not silently dropped:**
- `PretripChecklist` on the site detail page still runs on its default mock plan, not a real query joined to `dive_plans` — a meaningfully bigger "your whole plan" cross-site view than this page's single-site scope; natural next step now that `dive_plans` exists.
- Tide-station link (Optional/Bonus bucket) and the pin-tap "compact preview bottom sheet" (`creative/flows/map-exploration.md` nice-to-have) were not built — a direct link from the map pin to the site detail page covers this pass's scope.
- `T14.1`'s stubbed `DivePlanCheckInState` edge is not yet wired to the real `dive_plans` table (only the Safe-Return `running → checked-in` edge is real) — separate follow-up.

### Task 12: Intent-Driven Prefetch Pipeline (→ Task 12, → §2)
- [x] `T12.1` Supabase Storage bucket for offline bundle assets — `supabase/migrations/0004_offline_bundles_storage.sql`: creates the `offline-bundles` bucket (public read, 20MiB/object cap, JSON/image/PDF allowlist) + RLS on `storage.objects` (public read, no anon/authenticated write — same service-role-only gap as `camera_sources`' approve/reject). Applied and confirmed live 2026-08-08 — the bucket exists on the real project with the exact config specified (verified via the Storage admin API: 20MiB limit, correct MIME allowlist, `public: true`). What's still missing is the real upload route itself (`BUNDLE_SIGNING_SECRET` is still unset — see `T12.2`), not the bucket.
- [x] `T12.2` Server-side bundle signing/checksumming (manifest + hash) — `src/lib/offline/bundle-signing.ts`, HMAC-SHA256 via `node:crypto` over a canonicalized manifest, `timingSafeEqual` comparison. Secret read from `BUNDLE_SIGNING_SECRET` (added to `.env.local.example`, not set — generate with `openssl rand -hex 32` once `T12.1` lands). Crypto logic hand-verified against tamper/wrong-key/malformed cases.
- [x] `T12.3` **Decision:** Dexie.js confirmed as the IndexedDB wrapper (installed)
- [x] `T12.4` Client-side IndexedDB schema for cached site/hazard records — `src/lib/db.ts` (`cachedSites`/`cachedHazards`, both with a `cachedAt` timestamp)
- [x] `T12.5` Service worker runtime-caching strategy for site assets (extends Serwist config) — `src/app/sw.ts`, `StaleWhileRevalidate` for Mapbox tiles + site imagery, ahead of the default cache rules
- [x] `T12.6` "Prefetch Now" manual trigger UI — `src/components/prefetch-button.tsx` (writes a mock cached record for now; real bundle fetch/verify wiring is a TODO pending `T12.1`/`T12.2`). **Re-upgraded to `[x]` 2026-08-08:** mounted for real on the new site detail page (`T11.5`) — verified live, not just present in the diff.
- [x] `T12.7` Pre-trip checklist prompt — `src/components/pretrip-checklist.tsx`. **Re-upgraded to `[x]` 2026-08-08:** mounted for real on the site detail page (`T11.5`) — still running on its default mock plan, not yet joined to the real `dive_plans` table (explicit known follow-up, see `T11.5`'s notes), but reachable and correctly rendering, which is what this checkbox tracks.
- [x] `T12.8` Client-side bundle integrity verification — fail closed on a bad check — `src/lib/offline/bundle-verification.ts`. **Real today:** per-file SHA-256 checksum verification via `crypto.subtle.digest`. **Honest stub:** the HMAC *manifest* signature can't be verified client-side (the secret must never reach the browser) — `verifyBundleIntegrity` calls an injectable verifier defaulting to `fetch('/api/verify-bundle')`, which doesn't exist yet; a 404/network failure already resolves to `false` (fail-closed is correct today, nothing to fix once the route lands). Wired into `prefetch-button.tsx`: a failed check sets an error state and does not write to `db.cachedSites`.

### Task 13: Offline Sync Status UI (→ Task 13, → §2)
- [x] `T13.1` "Cached as of [time]" freshness indicator component — `src/components/freshness-badge.tsx`, live-updating (re-renders every 60s so fresh→stale is visible without a reload)
- [x] `T13.2` Staleness threshold logic (e.g. flag past 12h) — configurable prop on `FreshnessBadge`, defaults to 12h; missing/invalid `cachedAt` fails toward "stale," not "fresh"
- [x] `T13.3` Wire freshness indicator into site detail + map pin UI — homepage (`src/app/page.tsx`) still shows the `FreshnessBadge`+`ProvenanceBadge` pairing on mock cached-site entries (T13.3's original scope, unchanged), and `T11.5` separately wired the real `ProvenanceBadge`/`LegalAccessBadge` pairing into the site detail page and map pins for actual `sites` data. **Re-upgraded to `[x]` 2026-08-08.**

### Task 21: External Dive-Site Data Sourcing (→ Task 21, → plan.md Resolved Decision #6, new 2026-08-08, founder-directed)
Implements the founder's own framing ("if searching a new location with no data, use the API") via OpenStreetMap/Overpass — selected over the founder's other two proposed options after fact-checking found real, unresolved license/cost blockers on both (see plan.md Resolved Decision #6 and the Blocked-bucket entries in Priority Triage). **Interfaces below are pre-agreed so parallel agents build against the same contract** — reconcile against these signatures (not just each agent's own tests) before marking any of T21.1–T21.4 done, same lesson as this session's earlier migration-collision/duplicate-client incidents.
- [x] `T21.1` Migration `supabase/migrations/0008_sites_external_source.sql` — nullable `external_source text`/`external_id text` on `sites`, **partial** unique index on `(external_source, external_id)` where both non-null. No RLS change (service-role-only write path, same as every other automated-write table in this schema). Also added `0009_external_search_area_cache.sql` for T21.3's area-cooldown table (its own file, not folded into 0008 — see that migration's header comment for the reasoning), and updated `supabase/README.md`'s Files list, a new "External dive-site sourcing" section, and Open items #12/#13. **Not applied to any live project** — same founder-action-pending status as every other migration here; this pass verified the SQL by reading/reasoning (matching this repo's established style, referencing the exact partial-index/service-role-only patterns 0002/0005/0006 already use), not by running it — no live Supabase project exists in this environment.
- [x] `T21.2` `src/lib/sites/osm-import.ts` — `queryOverpassNearby`/`upsertSitesFromOsm` built to the pre-agreed exact signatures, reconciled against `T21.4`'s already-landed `nearby-dive-sites-list.tsx` (confirmed its `SearchNearbyResponse` shape matches T21.3's route exactly). `queryOverpassNearby` POSTs Overpass QL to `overpass-api.de` with an 8s `AbortController` timeout, derives `entryType` from `scuba_diving:entry`, never throws. `upsertSitesFromOsm` originally filtered to `entryType === "shore"` only, always writes `provenance: "COMMUNITY"` + `legal_access_status: null` + an ODbL-attribution `description`, and does a pre-check-then-insert (not `.upsert(...,{onConflict})`) against `external_id` — **judgment call:** the unique index is *partial* (`where external_source is not null and external_id is not null`), and Supabase/PostgREST's upsert helper generates a plain `ON CONFLICT (col1,col2)` with no way to supply that predicate, so it can't target a partial index; a race that slips past the pre-check is instead caught as a real Postgres `23505` and falls back to per-row inserts (see the file's own header comment for the full reasoning). 15 unit tests (`osm-import.test.ts`) against hand-built fixture Overpass JSON — entryType derivation (including "no tag" and "unrecognized tag value" both correctly falling to `unknown`, never guessed), the non-scuba/malformed-element filter, the shore-only filter, the dedup-by-external_id logic, the duplicate-race fallback, and every failure path (non-ok HTTP, network failure, bad JSON, Supabase errors, `createAdminClient` throwing) — all pass, no live network/DB. Also added `src/lib/sites/external-search-cache.ts` (T21.3's grid-rounding + cooldown math + the Supabase-backed claim function) with 13 of its own pure-logic tests, and a small additive `sortByDistanceWithinRadius` helper in `distance.ts` (5 new tests) so the search route reuses `distanceMiles` instead of re-deriving radius-filter/sort logic.

**Bug #1 found and fixed live, 2026-08-09 — filter too strict:** the founder searched from Fort Lauderdale post-migration and found `sites` still only had the 3 original CA rows, despite the UI reporting an external search happened. Root-caused by directly querying Overpass for that exact area (not guessed): 2 real Florida shipwrecks came back, both classified `"unknown"` entry type since neither carries a `scuba_diving:entry` tag — which turns out to be the norm across OSM's real data, not an edge case, meaning the "shore only" filter imported essentially nothing anywhere it was actually tried. **Widened `upsertSitesFromOsm`'s filter from `entryType === "shore"` to `entryType !== "boat"`** (imports shore + unknown, skips only confirmed boat-only) — accepted tradeoff documented in `plan.md`'s Resolved Decision #6 correction note (occasional untagged boat-only imports are possible, judged acceptable given `COMMUNITY` provenance + explicit unreviewed-data disclosure already in place, versus the old behavior of importing nothing at all). Both `osm-import.ts`'s doc comments and `osm-import.test.ts`'s two filter-behavior tests updated to match (renamed the misleading `shoreSites` variable to `importableSites` throughout).

**Bug #2 found and fixed live, same day — the real cause of every "406"/"504" seen all session was neither rate-limiting nor an IP block, it was a missing `User-Agent` header.** Live testing after Bug #1's fix kept hitting non-ok responses even with generous backoff; initially misread as Overpass fair-use throttling from repeated testing. Isolated by direct comparison: a plain `curl` to the identical endpoint with the identical query succeeded every time, while a bare Node `fetch()` with no other change 406'd — Overpass's Apache front-end was rejecting requests with no/a generic `User-Agent` via content-negotiation, unrelated to request volume or IP. Fixed by adding an explicit, descriptive `User-Agent` header to `queryOverpassNearby`'s fetch call — which is also literally what Overpass's own usage policy asks callers to do, so this is correctness, not a workaround. Both bugs verified: `tsc --noEmit`, lint, full 152-test suite, all clean.

**Real South Florida data live-populated same day**, via a one-off script (not part of the app) that calls the real, unmodified production `queryOverpassNearby`/`upsertSitesFromOsm` functions across 6 overlapping points spanning Key West → West Palm Beach (the on-demand route only populates where a real user has searched; this seeds it proactively). Result: **7 real sites inserted** — USNS General Hoyt S. Vandenberg (Key West), Cudjoe Gardens Marina And Dive Center + a Marathon dive shop, Spiegel Grove (Islamorada), Triumph Reef (Miami), S.S. Inchulva/Delray Wreck + Lofthus (West Palm Beach area) — plus 2 correctly skipped (1 duplicate across overlapping search circles, 1 confirmed boat-only). Confirmed directly against the live database via REST API, not inferred from script output alone. Fort Lauderdale's own search point specifically timed out twice (an 8s client-side abort, not an Overpass error) — a longer-patience targeted retry for that one point is a natural follow-up so the founder's own immediate area isn't solely covered by the neighboring circles' overlap.
- [x] `T21.3` `src/app/api/sites/search-nearby/route.ts` — local-first via `listSitesWithHazardFlag()` + `sortByDistanceWithinRadius`; below 3 in-radius local results, calls `claimExternalSearchWindow()` before ever calling Overpass, which degrades to "skip the external attempt, serve local data" (not an error) if the cache check itself can't be verified. An Overpass/import failure is logged and degrades to existing local data — never surfaces as the response's `error` field, never a 500; `error` is reserved for a genuine local-DB failure, matching `T21.4`'s already-landed consumer exactly (confirmed by reading `nearby-dive-sites-list.tsx`: it never inspects `error` from a 200 response, only reacts to a non-200/network failure by falling back to client-side filtering — consistent with this route only ever 400ing on bad params). `searchedExternally` reflects whether an attempt happened, independent of outcome. **Cache duration reconciled to plan.md's same-day correction (24h → ~30 days, Resolved Decision #6's amendment)** — implemented directly at ~30 days (`COOLDOWN_DAYS = 30` in `external-search-cache.ts`) rather than landing the original 24h number and fixing it in a follow-up, since the correction was already documented in `plan.md`/this file before this pass started. Verified via `tsc --noEmit`, lint, and the full test suite (143 passing, up from the pre-existing 109) — all clean — and `npm run build` (the route appears in the build's route list as `ƒ /api/sites/search-nearby`). No live Overpass/Supabase call was exercised end-to-end in this environment (no credentials here); that integration check is expected to happen after this report lands.
- [x] `T21.4` `src/components/nearby-dive-sites-list.tsx` now calls `GET /api/sites/search-nearby` once geolocation resolves to real coordinates, for any finite radius (re-fetches when the radius `<select>` changes); the "All" radius is deliberately never sent to the route (no sensible finite value to send — client-side filtering of the full `sites` prop already shows everything). Search state is tagged with the radius it was fetched for (`SearchNearbyState.radiusMiles`) so a radius change can't render one stale frame of the previous radius's server result under the new label. Loading: "Looking for dive sites near you…" shown alongside the still-visible fallback content (no layout-jarring swap). Zero-result empty state distinguishes `searchedExternally: true` ("Checked OpenStreetMap too — no dive sites within {radius} yet") from a plain local-only miss. Fetch failure (network error/non-200) is logged via `src/lib/sites/logger.ts` and falls back to the pre-T21.4 client-side filtering of the `sites` prop — never a visible error state. Verified: `tsc --noEmit`, lint, and the pre-existing 109-test suite all clean against my own change (T21.3's route doesn't exist in the filesystem yet as of this pass — parallel-agent dependency, so no live end-to-end fetch was possible; component logic verified against the documented contract only). **Reconciliation note resolved:** the `npm run build`/`tsc` repo-wide failure this entry flagged (from T21.2's then-in-flight `osm-import.test.ts`) is fixed as of the T21.1–T21.3 pass above — full `tsc`/lint/test/build all clean together as of this update, and `nearby-dive-sites-list.tsx`'s `SearchNearbyResponse` shape was confirmed to match the landed route's actual response exactly (`{ sites, error, searchedExternally }`), no drift.

**Explicitly not in this pass** (see plan.md's Blocked bucket): the RapidAPI commercial option (pricing unconfirmed — founder action to check), `dulcetgnome/divestop` (no license — needs author permission), and `cetra3/divedb`'s data-reuse question (AGPL code not used; whether its *data* is separately reusable is unresolved, not urgent).

**Added 2026-08-08, same day — plan-only, implementation explicitly NOT authorized yet** (founder: "No dev work needs to be done until I say it... this is for updating our plan"). Do not start T21.5/T21.6 without an explicit go-ahead, distinct from the "fan out and implement" authorization T21.1–T21.4 already had.

**Hold lifted for T21.5/T21.6/T21.7, same day (2026-08-08), founder's own words, quoted in order:** after the note above was written, the founder asked (verbatim) to update the plan for Mapbox's recommended GeoJSON-source/symbol-layer pattern at scale ("For the sake of speed... we want to make sure all those locations are identified in mapbox... GeoJSON & Symbol Layers... Best if you are handling hundreds or thousands of dive sites efficiently at scale... a symbol layer") — this became Resolved Decision #9. In the same turn, immediately after, the founder said: **"If we are coding the map, then fan out agents for adding this."** That is the explicit go-ahead, specifically for the map-coding batch (T21.5's `site_type` data layer, T21.6's shared radius state, T21.7's GeoJSON/symbol-layer migration — sequenced together per Decision #9's own text). T21.5's icon-set design-review note (below) is unaffected by this — the *data layer* (schema, OSM tag derivation) is unblocked; the map's actual icon *artwork* should still lean conservative/placeholder pending a real design pass, per that item's own text.
- [x] `T21.5` Site-type classification (`site_type` enum: `shore_reef`/`shipwreck`/`cave`/`spring`/`artificial_reef`/`unclassified`) + distinct map-pin icons per type (plan.md Resolved Decision #7). **Needs a design pass through `creative/AESTHETIC_REVIEW.md`'s process before implementation** — the map pin already carries 3 visual dimensions (provenance ring, hazard fill, legal-access corner glyph) and is at real risk of becoming unreadable with a 4th bolted on carelessly, the same failure mode the original pin legibility fix (`T11.5`) had to correct away from. **Data-layer half only, this pass — the map-icon-rendering half is separate, parallel, in-flight work (a different agent, working against this same `SiteType` contract; not verified here).** Built: `supabase/migrations/0010_sites_site_type.sql` — `site_type` enum (matching Decision #7's 6 values exactly) added `not null default 'unclassified'` (not nullable, unlike `legal_access_status` — `'unclassified'` already *is* the "no known category" state, so a separate NULL would be redundant), plus `sites_site_type_idx`. `supabase/README.md`'s Files list and Open items updated to match. `src/lib/sites/types.ts` now exports `SiteType` (the 6-value union) and both `Site`/`SiteMarker` carry `site_type: SiteType`. `src/lib/sites/queries.ts`'s `listSitesWithHazardFlag`/`getSiteWithHazards` select and pass through the new column. `src/lib/sites/osm-import.ts`'s `OsmDiveSite` gained `siteType`, derived by a new `deriveSiteType()` (mirrors `deriveEntryType`'s "under-classify rather than guess" discipline) and written through to `sites.site_type` on insert. **OSM tag mapping, live-verified against overpass-api.de this pass, not assumed:** `historic=wreck` → `shipwreck` (confirmed: USS Vandenberg, USS Spiegel Grove, several FL Keys wrecks); `natural=cave_entrance` → `cave` (confirmed: Devil's Den — notably tagged `cave_entrance`, not `spring`, despite being a "prehistoric spring" colloquially, which is why cave is checked before spring); `natural=spring` → `spring` (confirmed: Ginnie Spring, Ichetucknee Spring, Rainbow Spring/Rainbow Spring North — this is the tag `supabase/sources/dive-site-data-source-options.md` section 1's "16 freshwater springs" finding left unverified; now verified); `natural=reef` → `shore_reef` (confirmed real/live, Caribbean/Gulf region). **`artificial_reef` has no derivation case, deliberately:** `man_made=reef` is a real, actively-used OSM tag elsewhere (e.g. Windara Shellfish Restoration Reef, Virginia DWR's Christmas Tree/Tire Reefs), but a global Overpass query found zero nodes where it co-occurs with `sport=scuba_diving` — nothing in OSM's actual scuba-diving-tagged data distinguishes an artificial reef from a natural one today, so this value is reachable only via manual/founder curation, not invented as a shaky heuristic. `osm-import.test.ts` updated: the existing mixed-fixture test now also asserts `siteType`, plus 7 new cases (one per known tag mapping, `man_made=reef` explicitly falling to `unclassified`, and an unrecognized `natural=` value also falling to `unclassified`) — 150 passing, up from the 143 baseline. Verified clean: `tsc --noEmit`, lint (on the files this pass touched — a lint error and a warning existed in `site-map.tsx`/`dive-site-explorer.tsx` from the parallel T21.6/T21.7 pass at the time this note was written, not introduced by this pass), full test suite, `npm run build`. **Upgraded to `[x]` 2026-08-08**, same day: the map-icon-rendering half landed (`T21.7`, below) and both halves were independently reconciled together — full `tsc`/lint/150-test-suite/`npm run build` all clean with zero errors or warnings across the whole batch, confirming the `SiteType` contract holds end-to-end with no drift between the two parallel passes.
- [x] `T21.6` Map and `NearbyDiveSitesList` share one radius-driven data source (plan.md Resolved Decision #8) — built `src/components/dive-site-explorer.tsx`, a new client-side wrapper that owns geolocation resolution, `radiusMiles` state, and the `/api/sites/search-nearby` (T21.3) fetch exactly once (moved wholesale from `NearbyDiveSitesList`, not duplicated), feeding both the migrated `SiteMap` (T21.7) and the list from the same computed `inRadius` result. `src/app/page.tsx` is unchanged as a Server Component doing the initial full-list fetch; it now renders `<DiveSiteExplorer sites={sites} />` in place of the old independent `<SiteMap>`/`<NearbyDiveSitesList>` siblings. `NearbyDiveSitesList` is now purely presentational (props: `sites`, `status`, `coords`, `radiusMiles`, `onRadiusChange`, `radiusOptions`, `isSearching`, `inRadius`, `searchedExternally`) — same rendering/copy/empty-state behavior as before, verified by the unchanged 150-test baseline (no test file existed for either component pre- or post-change). `SiteMap` keeps its own independent `useGeolocation()` call for `T11.5.8`'s recenter `flyTo` — deliberately not threaded through the new shared state, since that's a separate, already-working concern this task doesn't touch (same duplication that already existed pre-T21.6, just relocated). Map/list now visibly share pins: before geolocation resolves or when radius is "All", both fall back to the same full `sites` prop; once resolved, both read the same `inRadius` (server search result or client-filtered fallback).
- [x] `T21.7` Migrated site-pin rendering in `src/components/site-map.tsx` from one React `<Marker>` per site to a GeoJSON `Source` + symbol `Layer` (plan.md Resolved Decision #9), using react-map-gl's declarative `<Source>`/`<Layer>` (not raw `map.addLayer()`). Icons: `map.loadImage()` wasn't used (callback-only in this mapbox-gl version, and these are generated locally, not fetched) — each of the 6 `SiteType` glyphs (imported from `@/lib/sites/types`, landed by the parallel T21.5 pass mid-session with the exact same 6 placeholder values this task's brief specified, so no reconciliation needed beyond swapping an import) is hand-drawn with Canvas 2D primitives, composited with the existing teardrop body (`SITE_PIN_PATH`, untouched — provenance ring pattern + hazard fill preserved exactly) and the legal-access corner badge, rasterized to `ImageData` via an offscreen `<canvas>`, and registered with `map.addImage()` for all 72 (site_type × provenance × hazard × legal-tier) combinations on map load; each GeoJSON feature carries a precomputed `icon` property consumed by a data-driven `"icon-image": ["get", "icon"]` layout expression. **Pin visual complexity — landed conservative, not a design pass:** kept the existing 3-dimension pin (provenance ring, hazard fill, legal corner) exactly as verified by the T11.5 legibility fix, and added the site-type glyph as a single small white outline icon inside the pin head, same visual weight for every type, per TASKS.md's own hold-lifted note that icon *artwork* should stay conservative/placeholder pending a real `creative/AESTHETIC_REVIEW.md` pass. This is a real 4th dimension bolted onto an already-tuned pin — genuinely untested against a live glance-legibility review (no browser in this environment); flagging plainly rather than asserting it reads well at real (~30x38px) size. Interaction: `map.on('click'/'mouseenter'/'mouseleave', LAYER_ID, ...)` on the `MapRef` inside a `useEffect`, matching this file's existing `flyTo` imperative-access pattern; click reads `feature.properties.id` and calls `router.push`. Accessibility: added a visually-hidden (`sr-only`) `<ul>` of every currently-rendered site's name+link, co-located inside `SiteMap` itself (not relying on `NearbyDiveSitesList` being rendered alongside it, since `SiteMap` is reused standalone) — same accessible-name text the old per-`<Marker>` `aria-label`/`title` carried. Verified: `tsc --noEmit`, lint, full test suite (150 passing, unchanged by this pass), `npm run build` all clean. No live Mapbox token/browser in this environment — symbol-layer rendering, icon legibility, and click/hover behavior are unverified live; expected to be checked via `agent-browser` next.

**Independent verification, 2026-08-08, same day (not the implementing agents' own reports):** read every new/changed file for T21.1–T21.7 in full; `tsc --noEmit`/lint/150-test-suite/`npm run build` all clean from a fresh shell. Found and fixed one real bug live: `errorMessage()` (used throughout the Task 21 backend) treated Supabase/PostgREST errors as non-`Error` objects and logged `"[object Object]"` instead of the real message — caught by deliberately exercising the fail-closed path (`external_search_area_cache` not yet applied) live and reading the resulting log line. Consolidated the fix into a new shared `src/lib/error-message.ts` and applied it across all 14 call sites in the codebase that had the same latent bug (not just the 3 this session's own new code introduced), per the founder's explicit "we always want clean code" direction. Also live-tested the search-nearby route's local-only and thin-area-fallback paths against the real (not-yet-migrated) Supabase project — both behave exactly as designed.

**Regression found live, then resolved same day:** before migrations were applied, `site_type` being added to `listSitesWithHazardFlag`/`getSiteWithHazards` (the *core* site-listing queries, not just new Task 21 paths) meant the homepage's map and list went fully empty — a real step down from the pre-Task-21 baseline, confirmed live via `agent-browser` ("Couldn't load dive sites from Supabase (column sites.site_type does not exist)"). The failure mode itself was correct throughout (an honest error banner, never a crash or fake data) — but the practical effect was real breakage. **Resolved 2026-08-08, same day:** the founder applied migrations `0007`–`0010` via the SQL editor. Confirmed directly against the live project's REST API before trusting anything downstream (`site_type` present and returning `"unclassified"`, `external_source`/`external_id` present, `external_search_area_cache` and `dive_plans` tables both exist) — not inferred from the app merely working again.

**Full live re-verification after migrations landed, 2026-08-08 (a genuinely fresh dev server — a stale process on port 3000 from earlier testing was found and killed first, to avoid trusting a pre-migration connection):**
- Homepage correctly renders all 3 real sites again, map and list both — the regression is gone.
- Map pin click, at real pixel precision (`agent-browser mouse move`/`down`/`up`, not the imprecise `click` selector — a symbol layer has no DOM element to target by selector): cursor correctly shows `pointer` on hover over the site-pins layer, click correctly navigates to `/sites/<real-id>`, and the destination page renders the correct site (La Jolla Cove, badges/description/hazard-reports section all correct).
- Site-type icon rendering: a cropped/upscaled screenshot of a live pin shows the "unclassified" glyph (a plain white circle, correctly composited into the existing teardrop body with its provenance-pattern ring and the amber MPA corner badge) actually present and legible under inspection — not just claimed from source reading.
- The real Overpass search pipeline: a Fort Lauderdale search (thin local data) was confirmed via server log to have actually fired a real external search (`search_nearby`/cache-claim trace), and the UI correctly showed "Checked OpenStreetMap too — no dive sites within 25 mi yet". A second identical request, made from a freshly-restarted server minutes later, correctly hit `external_search_cache.on_cooldown` and skipped the redundant call — UI correctly reverted to the plain (non-"checked OpenStreetMap") empty-state message, proving the honest-messaging distinction tracks the *real* search outcome, not just a hardcoded string.
- Map/list radius sync (`T21.6`'s actual fix): selecting "All" radius while centered on Fort Lauderdale (zero nearby real sites) was confirmed, via the map's own visually-hidden accessible site list (`sr-only <ul>`, fed by the identical `siteMarkers` prop as the GeoJSON source), to now include all 3 California sites — proving the map's underlying data updated with the radius change, not just the list beside it. The pins aren't visible on-screen in that view purely because California is ~2,300mi outside the Fort Lauderdale viewport, not because the sync failed.

Nothing about this batch remains unverified except the icon system's real-world glance-legibility at true pin size, which is explicitly a design-review question (`creative/AESTHETIC_REVIEW.md`), not a functionality gap — flagged honestly rather than asserted as settled.

### v5.3 batch, 2026-08-09 — real South Florida data + filter-by-type

- [x] `T21.8` **OSM description enrichment** ("this should include all data") — `OsmDiveSite` gained `osmDescription`/`depth`/`website`, extracted from the OSM node's own real tags (`normalizeElement`). `buildOsmDescription` now leads with the source's genuine free-text content when present (real example, live-verified: the Delray Wreck's actual OSM description, a specific, human-written paragraph — previously discarded entirely in favor of only a generic attribution sentence), with depth/website appended as their own scannable lines and the ODbL attribution/disclosure sentence always kept, never dropped. No schema change (`sites.description` already existed) — 2 new tests, 152 passing total.
- [x] `T21.9` **Filter dive sites by type** ("we can have tags in the dive sites and be able to filter by tag") — built on the existing `site_type` column, no new migration. New `src/lib/sites/site-type-labels.ts` (shared label/order mapping). `DiveSiteExplorer` owns `siteTypeFilter` state (same shared-state-in-the-parent pattern as radius, `T21.6`) and filters both `mapSiteMarkers` and what's passed to the list from one source — a filter that only touched the list would've recreated the exact bug `T21.6` fixed. `NearbyDiveSitesList` renders a `SiteTypeFilterRow` (chips: All + each `SiteType`) and reports selection changes upward. **Live-verified, both directions:** selecting "Reef" against real Fort Lauderdale data (2 real shipwrecks, 0 reefs) correctly emptied the map and showed "No reef sites within 25 mi of your location yet"; selecting "Shipwreck" correctly showed both real sites. One real testing-tool gotcha hit and resolved during verification: `agent-browser click @ref` intermittently failed to register on these chip buttons (coordinate-precision issue with the tool, not the app — confirmed by dispatching a native `element.click()` via `eval`, which worked immediately); noted here in case it recurs testing other small inline controls.
- [x] `T21.10` **Root-caused and fixed the real reason every Overpass call was failing** — not rate-limiting or an IP block (the working theory during live debugging), but Overpass's Apache front-end 406-rejecting requests with no/a generic `User-Agent`, isolated by direct comparison (`curl` with the identical query succeeded every time; a bare Node `fetch()` 406'd; adding `User-Agent: curl/8.7.1` alone fixed it in isolation). Fixed properly in `queryOverpassNearby` with a real, descriptive User-Agent identifying the app — which is also literally what Overpass's own usage policy asks callers to do.
- [x] `T21.11` **Bulk-populated real South Florida coverage, Key West → West Palm Beach** — the on-demand route only ever populates where a real user has searched; a one-off script (`/private/tmp/.../scratchpad/populate-south-florida.ts`, not part of the app) called the real, unmodified `queryOverpassNearby`/`upsertSitesFromOsm` across 6 overlapping points spanning the corridor, plus a targeted retry for Fort Lauderdale specifically (its first two attempts hit an 8s client-side timeout, unrelated to the User-Agent fix — Overpass queries for that specific area were just slower). **Result, confirmed directly against the live database via REST API, not inferred from script output: 8 real sites inserted** — USNS General Hoyt S. Vandenberg + Cudjoe Gardens Marina And Dive Center (Key West), a Marathon dive shop, Spiegel Grove (Islamorada), Triumph Reef (Miami), SS Copenhagen Shipwreck + S.S. Inchulva/Delray Wreck (Fort Lauderdale/Delray), Lofthus (near West Palm Beach) — spanning 24.45°N to 26.56°N, real coverage across the whole named corridor, not just one city. All correctly `COMMUNITY` provenance, `legal_access_status: null`. 2 candidates correctly skipped (1 cross-circle duplicate, 1 confirmed boat-only).

- [x] `T21.12` **Bounded site queries — fixed a real silent-truncation bug, not a perf nit** (`src/lib/sites/queries.ts` only). `listSitesWithHazardFlag()` selected *every* row of `sites` **and** every row of `hazard_reports` with no `.limit()` and no geographic filter (callers then radius-filtered in JavaScript), on every homepage render (`force-dynamic`) and once or twice per `/api/sites/search-nearby` request. PostgREST silently caps result sets at its own `max-rows` (Supabase default 1000) with **no error and no signal** — so once `T21.11`'s import pipeline grows `sites` past that, dive sites would simply stop appearing on the map with nothing indicating rows were dropped. In a hazard-surfacing app that's a correctness/safety failure, so it's fixed in both directions: **bound the query, and never let a bounded result pass as a complete one.** New `listSitesInBounds({north, south, east, west, limit?})` filters server-side (`.gte`/`.lte` on `latitude`/`longitude`) with an explicit `.limit()` (`DEFAULT_SITE_QUERY_LIMIT = 500`, clamped to `MAX_SITE_QUERY_LIMIT = 1000` — above PostgREST's own ceiling the number would be a lie), and scopes its hazard read with `.in("site_id", <ids just returned>)` + `HAZARD_FLAG_QUERY_LIMIT = 1000` instead of a second full-table read (skipping that round trip entirely when zero sites matched). **Antimeridian handled, not documented-around:** a box crossing ±180° has `west > east`, which as a `.gte`/`.lte` conjunction is unsatisfiable and would return zero rows *silently* for every Pacific user — detected and expressed as `or=(longitude.gte.<west>,longitude.lte.<east>)`, still ANDed against the (non-wrapping) latitude bounds. `listSitesWithHazardFlag()` is deliberately **kept** (the homepage is a Server Component; geolocation is browser-only, so there are genuinely no coordinates server-side) but both its queries now carry explicit limits. Truncation is surfaced, not swallowed: `ListSitesResult` gained `truncated: boolean` (additively — existing `{ sites, error }` destructuring still compiles, and no call site was changed by this pass) set when either read comes back holding exactly its limit, plus a structured `logger.warn`. Note the hazard read is the direction that matters most: a truncated hazard result would drop a real report's flag, i.e. fail toward looking *safer* than reality — so it trips `truncated` on its own, independent of the sites read. Malformed bounds (non-finite edge, inverted north/south) return an `error` **before** any query rather than a zero-row answer indistinguishable from "no sites here". New `src/lib/sites/queries.test.ts` — 27 tests against a recording mock of the Supabase builder (asserting *which* filters/limits were issued, since row assertions alone can't distinguish a server-side filter from a full-table read plus JS filtering): bounding filters applied, limit applied/passed through/clamped both ways, hazards scoped to the returned ids, truncation at the exact limit boundary (both reads), both antimeridian assertions, invalid-bounds short-circuit, and every existing error path still returning `{ sites: [], error }` rather than throwing. Verified: `tsc --noEmit`, lint, full suite (193 passing, up from 152 — includes a parallel agent's tests landing in the same tree), `npm run build` all clean. No live DB used. **Not wired up:** call sites (`src/app/page.tsx`, `src/app/api/sites/search-nearby/route.ts`) are deliberately untouched — `listSitesInBounds` exists but has no consumer yet; wiring it is a separate follow-up. Also still unbounded, flagged not fixed: `getSiteWithHazards()`'s per-site hazard read (single-site scope, far lower risk).

**Full live re-verification after all of the above, same day:** fresh dev server (killed a stale process still lingering from earlier testing first — confirmed via `lsof`, not assumed), Fort Lauderdale geolocation patched via `agent-browser`. Homepage correctly shows the 2 real nearby sites (SS Copenhagen 6.7mi, Delray Wreck 23.4mi) sorted by distance, filter chips render and both empty/positive filter states verified as described in `T21.9`. `tsc --noEmit`, lint, full 152-test suite, and `npm run build` all clean as of this final pass.

- [x] `T21.13` **Closed a real, verified hole: `GET /api/sites/search-nearby` was a public, unauthenticated trigger for service-role writes.** Below the 3-local-result threshold the route called Overpass and imported new `sites` rows via the RLS-bypassing admin client, with the *only* throttle being `external-search-cache.ts`'s per-grid-cell cooldown — which keys on a rounded 0.5° cell (~259,200 of them globally), so an anonymous script that varies its coordinates never revisits a cell and never trips it. Net effect: unbounded anonymous-triggered writes into `sites`, plus fair-use abuse of Overpass's free public instance (the kind that gets a deploying IP banned). `0006_rate_limiting.sql` couldn't have caught it either — its trigger isn't attached to `sites` and keys on `auth.uid()`, which is NULL for service-role writes. **Two layers, both on the import side effect only — local reads stay fully public** (`0002_rls.sql`'s public-read model is the product, breaking it would be the worse regression): **(1)** the external branch now requires a signed-in user via the same `createClient` + `auth.getUser()` pattern as `api/dive-plans/route.ts`, resolved by a non-throwing `getAuthenticatedUserId()` helper called *only* on the thin-area path (a plain public map read pays no auth round trip) — an anonymous request is **not** an error: still a 200, still local results, `searchedExternally: false`, with an `search_nearby.external_skipped_unauthenticated` info log; fails closed to "anonymous" if the auth check itself throws (missing env vars etc.), since an unverifiable identity must never unlock a service-role write path. **(2)** `DAILY_EXTERNAL_SEARCH_CAP = 100`, a global rolling-24h ceiling across all users and all cells, enforced inside `claimExternalSearchWindow` after the (cheaper) cell lookup so an already-on-cooldown area doesn't pay for a count query. **No migration needed** — deliberately derived from the existing `external_search_area_cache` table: it already holds exactly one row per claimed cell and a cell can't be re-claimed inside its ~30-day cooldown, so "rows with `last_searched_at` in the last 24h" is an *exact* count of claims in the window, with no second source of truth to drift. Cap number is conservative-not-sacred (documented as such on the constant): organic use is nowhere near 100/day, a scripted coordinate sweep blows past it in minutes. Fails closed on a thrown, null, NaN, or negative count (`reason: "count-unavailable"`), matching `claimExternalSearchWindow`'s existing posture and `webcam-extraction/rate-cap.ts`'s injectable-counter shape. 14 new unit tests in `external-search-cache.test.ts` (cap boundary at/under/over, zero-count, all three fail-closed count shapes, default-cap resolution, window-start math, and an explicit "a never-searched cell still can't exceed the global cap" case proving the two limits are independent) — **166 passing, up from 152**. Verified: `tsc --noEmit`, lint, full suite, `npm run build` all clean. Not exercised live end-to-end (Overpass slow today, test-area cells already claimed) — the cap/auth decision paths are pure and unit-covered instead.

- [x] `T21.14` **Wired the two hardening changes together** (the integration `T21.12`/`T21.13` were each deliberately forbidden from doing, since both would otherwise have edited `search-nearby/route.ts` concurrently — the collision pattern that cost reconciliation time earlier in this session). Added `boundingBoxForRadius(center, radiusMiles)` to `src/lib/sites/distance.ts` (the existing home for geo math) and switched the route's two `listSitesWithHazardFlag()` reads to `listSitesInBounds(bounds)`. **The correctness-critical property is that the box is always a *superset* of the radius circle** — a box even slightly too small silently drops sites genuinely in range, which is the same invisible-data-loss failure `T21.12` exists to fix, just moved one layer up. Three deliberate safeguards: miles-per-degree is rounded *down* (69) so both spans come out marginally wide; the longitude span is sized off whichever box edge sits furthest from the equator (degrees of longitude shorten poleward, so the center latitude would under-cover the poleward edge); and near-pole/`cos→0` and >half-globe radii widen to the full -180..180 range instead of dividing by ~0. Latitude clamps at ±90; longitude wraps, deliberately producing the `west > east` signal `listSitesInBounds` already detects for antimeridian boxes. 9 new tests in `distance.test.ts` including a property test that walks the compass every 5° at the radius edge (probing with a *larger* miles-per-degree than the implementation uses, so the assertion isn't trivially satisfied) and asserts every point falls inside the box — run at mid-latitude, the equator, 71°N, and across the antimeridian. **Live-verified no regression against real data:** Fort Lauderdale at 25mi returns the same 2 sites and at 250mi the same 8 South Florida sites as before the change (byte-identical result sets), a mid-Atlantic query correctly returns 0, an anonymous request still gets a 200 with local data and `searchedExternally: false` (logged as `search_nearby.external_skipped_unauthenticated`), and the homepage renders both real Fort Lauderdale sites with no console errors. Also renumbered the security entry to `T21.13` — both parallel agents independently claimed `T21.12`. Full suite **202 passing** (up from 152 at the start of this review batch); `tsc`, lint, build all clean.

- [x] `T21.18` **Exploration state now survives navigation** — three symptoms the founder reported (2026-08-09), all one root cause: the homepage always opened on the hardcoded California fallback and then visibly jumped to the diver's real location; the radius selector reset to 25mi on every return; and map panning/zooming was lost. All of it lived in component-local `useState` (plus a hardcoded `initialViewState`) inside `DiveSiteExplorer`/`SiteMap`, which App Router unmounts on navigation. New `src/lib/sites/explorer-preferences.ts` persists `{viewport, radiusMiles, siteTypeFilter}` to localStorage using the **`useSyncExternalStore` module-cache pattern already established** by `use-safe-return-timer.ts`/`use-post-dive-prompt-settings.ts` — not a `useState` + mount-effect flag, because the server has no localStorage and that would reintroduce exactly the hydration-mismatch bug documented in `use-geolocation.ts`; `getServerSnapshot` reconciles it. **Three non-obvious things this had to get right:** (1) Mapbox reads `initialViewState` *once* at mount and ignores later changes, so the map render is gated on `isHydrated` behind a same-size placeholder — otherwise the saved viewport loses the race and you get the California-then-jump flash this fixes. (2) The geolocation auto-fly now only runs when there is **no** saved viewport (guarded by both `isHydrated` and a `hasPositionedRef`), so a position the diver deliberately panned to isn't yanked away on the next mount — an explicit choice beats a first-run convenience. (3) `Infinity` (the "All" radius) does **not** survive `JSON.stringify` — it serializes to `null`, which silently restored as the 25mi default; persisted as an `"all"` sentinel instead. Persisted viewports are validated field-by-field on read (finite, in-range lat/lng/zoom) because localStorage is user-writable and survives deploys, and a NaN coordinate reaching Mapbox throws inside the GL library rather than degrading. Writes are on `onMoveEnd` (once per gesture), not `onMove` (per frame). 14 new jsdom tests in `explorer-preferences.test.ts` covering the `Infinity` round-trip, corrupt JSON, each invalid-viewport shape, partial corruption, and cross-tab sync. **Live-verified via genuine navigation round trips** (not just unit tests): with geolocation stubbed to Fort Lauderdale, set radius 100 + filter Shipwreck, clicked through to a real `/sites/<uuid>` detail page and back — radius, filter, and the Florida viewport all restored, with no California flash; and separately, a manual pan survived the same round trip byte-identically, proving geolocation no longer overrides it. `tsc`, lint, 309 tests, build all clean.
- [x] `T21.15` **Closed the repo's highest-risk coverage gap: the Safe-Return timer had zero tests.** `CLAUDE.md` names it one of two life-safety-critical areas, yet all 202 pre-existing tests covered pure helpers only — because no DOM test environment existed, so a hook test was literally unwritable. Installed `jsdom` + `@testing-library/react` + `@testing-library/dom` (dev-only, free — within the no-paid-dependency rule). **Environment split:** default stays `node` so the existing pure-logic suite is untouched; DOM tests opt in per-file via a `// @vitest-environment jsdom` docblock — `environmentMatchGlobs` was removed in Vitest 4, and a docblock keeps each file's environment visible at the top of that file rather than in a glob list elsewhere (`test.projects` remains the escape hatch if DOM tests ever need distinct setup). **24 tests for `use-safe-return-timer.ts`**: the `firedRef` double-fire guard (a repeating false alarm is the failure mode that would actually hurt a diver), reopen-after-expiry-while-backgrounded firing immediately on foreground, check-in/reset/silence transitions, localStorage round-trip plus the documented corrupt-JSON fallback, storage-write failure degrading in-memory, and cross-tab sync. **33 tests for `alert-channel.ts`**, concentrated on the degradation paths that *are* the product's honest-disclosure promise: permission denied, APIs entirely absent, `stop()` safe when idle or called twice. **Real finding, deliberately NOT fixed:** persisted `status: "running"` with `startedAt: null` passes `readPersisted()`'s validation (it type-checks `startedAt` but doesn't reject the null-while-running *combination*), `remainingMs` short-circuits to 0, and the expiry effect reads that as "countdown over" and **fires the alarm on load** — a false alarm from corrupted storage. Current behavior is pinned by a test; changing it to reset-to-idle is a founder call on a safety-critical path, not an agent's. Verified: `tsc`, lint, full suite, build all clean.
- [x] `T21.16` **Closed the last two defense-in-depth gaps this review batch left open** — one DB-side, one in `getSiteWithHazards()`. **(1) Rate-limit trigger attached to `sites`** (`supabase/migrations/0011_sites_rate_limit.sql`). `0006_rate_limiting.sql` attached `enforce_submission_rate_limit()` to `hazard_reports`/`lds_status`/`camera_sources` but deliberately scoped `sites` out, because it "has no live self-service submission UI yet" — reasoning that aged out: `0002_rls.sql`'s `sites_insert_own` already grants every authenticated user `insert` on `sites` (capped at `COMMUNITY` provenance, but **uncapped in volume**), and "no UI exists" was never an access-control argument when the Supabase client SDK is callable directly from any signed-in browser session with no server route in between. `sites` is also the one table designed to grow continuously and unattended (OSM import) and the one every map pin reads from. No new logic — one `create trigger` reusing 0006's `('created_by', 10, 5)` arguments, exactly as that migration's header predicted. **The nuance that made this safe, verified by reading the function body rather than assumed:** the trigger does *not* call `auth.uid()` — it reads the submitter off the row (`to_jsonb(NEW) ->> user_column`) and short-circuits `return new` when that's NULL (0006's documented fail-open branch, "an anti-abuse control, not a safety-critical one"). The OSM import (`src/lib/sites/osm-import.ts`) writes via the service-role client and its `toInsertableRow` never populates `created_by`, so for that path the trigger is a **silent no-op** — it cannot raise and cannot break the import. That path is correctly left uncovered because T21.13 already bounds it *upstream* twice (signed-in user required; `DAILY_EXTERNAL_SEARCH_CAP = 100` global rolling-24h), and there's no per-user attribution on an imported row to count against anyway. Also adds `sites_created_by_created_at_idx`, **partial** on `created_by is not null` — the trigger only ever counts non-null submitters, so the OSM-imported bulk (the part that grows) stays out of the index; 0006 added no index for its three small human-write-only tables, but a growing table would make the count a seqscan on every insert, including imports that pay it only to hit the fail-open branch. **⚠️ NOT APPLIED — founder action required:** no DB credentials exist here; run `0011` manually in the Supabase SQL editor *after* `0006` (it depends on the function existing). Until then `sites` still has no insert throttle at all. Logged as open item 13 in `supabase/README.md` with a smoke test. **(2) `getSiteWithHazards()` hardened** (`src/lib/sites/queries.ts`, that function only — `listSitesInBounds`/`listSitesWithHazardFlag` untouched). Both reads used `select("*")`, which is why this function *silently survived* the same `site_type` schema/code mismatch that made its sibling `listSitesWithHazardFlag()` fail loudly earlier today: `*` returns whatever columns happen to exist and the `RawSiteRow` cast is erased at runtime, so a read backing a hazard page couldn't distinguish "schema changed" from "fine." Replaced with explicit `SITE_DETAIL_COLUMNS` (all 11 `Site`/`RawSiteRow` fields; deliberately *excludes* `external_source`/`external_id` — real columns, but import bookkeeping not on the type or rendered anywhere) and `HAZARD_REPORT_COLUMNS` (all 7 `HazardReport` fields), following `SITE_MARKER_COLUMNS`' existing one-constant pattern. Verified against the live consumer `src/app/sites/[id]/page.tsx` — every field it renders is in the list. The hazard read was also unbounded (flagged-not-fixed by `T21.12`); now `SITE_DETAIL_HAZARD_LIMIT = 200`, a deliberately *tighter* sibling of `HAZARD_FLAG_QUERY_LIMIT = 1000` rather than a reuse: this read pulls whole rows including prose `description` into a Server Component payload (the flag reads pull one uuid), and its scope is a single site, where 200 reports is already far past plausible. Truncation direction is chosen, not incidental — the existing `order("created_at", desc)` means hitting the ceiling drops the *oldest* reports, never the newest, and the order/limit pairing is now documented as load-bearing. `SiteDetailResult` gained `truncated: boolean` **additively** (existing `{ site, hazards, error }` destructuring still compiles; no call site changed, same precedent `T21.12` set) plus a `sites.detail_truncated` structured warn, matching `sites.list_truncated`/`sites.bounds_truncated`. 17 new tests in `queries.test.ts` (recording mock extended with `eq` + a `maybeSingle` terminal): exact column lists asserted set-wise for both reads, `*` explicitly rejected, page-rendered fields covered, non-`Site` columns absent, both reads scoped to the id, limit + newest-first ordering issued, limit-boundary truncation with its structured log, numeric-as-string coercion, "no such site" vs. failure distinguished, and all three error paths returning a result rather than throwing. Full suite **219 passing** (up from 202, all 17 mine); `tsc --noEmit`, lint, build all clean. No live DB used.

- [x] `T21.17` **Extracted the map pin icon system out of `site-map.tsx` into `src/lib/sites/pin-icons.ts`** — a pure refactor ahead of the known design pass on the pins (plan.md Resolved Decision #7 / `creative/AESTHETIC_REVIEW.md`), which is much easier to act on against an isolated, testable module than against artwork buried in a 599-line component that also does Mapbox event wiring and JSX. **Boundary: the new module owns what icons exist, what they're named, and what they look like** (`SITE_PIN_PATH`, `SITE_TYPES`, `SITE_TYPE_GLYPHS`, `PinIconSpec`, `pinIconName`, `PIN_WIDTH`/`PIN_HEIGHT`/`PIN_RASTER_SCALE`, `drawPinIcon`, `allPinIconSpecs`, `legalGlyphTier`) and imports nothing from Mapbox/React/GeoJSON; `site-map.tsx` keeps how those icons *reach the map* (the `map.addImage` rasterization loop, `buildSiteFeatureCollection`, the symbol layer's `["get", "icon"]` expression, click/hover wiring, JSX). `legalGlyphTier` moved with the icon code — it defines the corner-glyph dimension — and is imported back for `siteAccessibleLabel`. All of the explanatory comments moved with the code they explain, including the 2026-08-08 legibility fix (provenance carried by stroke *pattern*, not hue, after two review lenses found the hue version unreadable at ~30x38px) and the "icon artwork is deliberately conservative pending a real design pass" note — that reasoning is the record of prior review findings, not decoration. **Behavior verified unchanged three independent ways, none of them "I read the diff":** (1) the pre-refactor code was recovered from the `.next` dev bundle built before the edit, and compiling the new module to plain JS and normalizing both (comments/whitespace/`export` stripped) gives **byte-identical** output, 3060 chars each — this covers `drawPinIcon`'s full canvas call sequence and every hand-drawn glyph coordinate, not just the pure logic; (2) an ordered snapshot of all 72 icon names + specs + every `legalGlyphTier` mapping was captured from the original source *before* editing and re-asserted against the new module at runtime — identical, including registration order; (3) the shipped, minified client chunk's pin-icon region is identical before/after the refactor under both identifier-stripped structural comparison and an in-order literal-sequence comparison (70 literals each). **20 new tests** in `pin-icons.test.ts`, pure-logic only, running in the existing Node vitest environment: name determinism/uniqueness/no-collision across the full space, names valid as Mapbox `icon-image` ids, spec count derived from the input dimensions rather than hardcoded to 72 (so a fifth dimension doesn't silently pass a stale assertion), a glyph present for every `SiteType` with no drift between `SITE_TYPES` and `SITE_TYPE_GLYPHS`, every `LegalAccessStatus` mapped to the right tier with `open` and `null` both explicitly yielding no glyph, and an end-to-end check that every combination a real `SiteMarker` could produce resolves to a registered icon name (a miss there is an invisible dive site on the map). **`drawPinIcon` is deliberately untested** — it needs a real Canvas 2D context (`Path2D`, `getContext("2d")`), which the Node environment doesn't have and jsdom only gets via an extra native dependency this no-budget project hasn't taken on; pixel assertions on placeholder artwork would also be rewritten by the upcoming design pass without having caught a real bug. Its correctness rests on verification (1) above instead. `site-map.tsx` **599 → 372 lines**. Suite **262 passing** (202 before, +20 here, the rest from two parallel agents); `tsc --noEmit`, lint, and `npm run build` all clean.

- [x] `T21.19` **Fixed the false-alarm-on-load bug `T21.15` found and pinned — founder call made: reset to idle, and say so.** A persisted `status: "running"` with `startedAt: null` passed `readPersisted()`'s validation (it type-checked `startedAt` individually but never rejected the null-while-running *combination*), `remainingMs` short-circuited to 0, and the expiry effect read that as "the countdown is over" and **fired the alarm on load** — a blaring "you didn't check in" for a dive the diver never started. **Why not keep firing:** with no usable `startedAt` there is no recoverable timer, so firing isn't failing safe — it asserts something specific and false ("your timer expired") when the truth is "we lost your timer state," and false alarms are exactly how a diver learns to distrust the one alarm that matters. **Why not reset silently:** a diver who really did have a timer running would see a normal idle screen and assume they were still covered — trading a false alarm for a false sense of safety, which is worse. **Corrupt combinations now rejected** (`unrestorableRunningReason()`, only ever applied when `status === "running"`): `startedAt` null *or* non-finite (`Infinity`/`-Infinity` — a hand-edited record can carry one, and it made `remainingMs` NaN, which is also `> 0 === false`, i.e. it fired too), and `durationMs` non-finite or `<= 0` (a zero/negative window is already "over" the instant it's read). Non-running statuses are untouched, keeping the existing lenient coercion. Each rejection returns `DEFAULT_SNAPSHOT` **before** anything reaches the expiry effect and logs `safe-return.corrupt-running-state-recovered` with a `reason` (`missing-started-at` / `invalid-duration`) plus the raw persisted values, alongside the existing `safe-return.storage-read-failed` precedent. The reset is also **written back** to localStorage (a one-shot effect on the recovery path) so the unrestorable record can't be re-read on the next load. **UI:** new `src/components/safe-return/recovery-notice.tsx`, rendered above `DisclaimerNotice` on the start screen — *"A previous timer couldn't be restored / We found a saved timer from an earlier session, but part of it was missing, so there was no countdown left to pick up. No alarm was triggered, and no timer is running now. If you're still diving, start a new one below."* Sky/informational palette with a `Dismiss` action, `role="status"` (polite, not `role="alert"`) — deliberately **not** amber (that's the standing safety disclaimer's language and shouldn't be diluted by an informational one-off) and never red; the word "error" is avoided entirely, since this isn't a failure the diver caused. **Non-permanent three ways:** an explicit Dismiss, cleared automatically by `start()` (once there's a real timer again the notice is stale) and by `reset()`, and it can't reappear on reload because the storage record was repaired. **Additive API only** — `recoveredFromCorruptState: boolean` + `dismissRecoveryNotice()` on `UseSafeReturnTimerResult`, so existing consumers still compile. The flag lives in a *transient* module-scope variable read through its own `useSyncExternalStore` (sharing the existing `listeners` set), deliberately outside `SafeReturnTimerSnapshot` so it can never ride along into `commit()` — a notice about lost state must not itself become saved state. **All 23 other timer tests pass untouched** (`firedRef` double-fire guard, reopen-after-expiry-while-backgrounded, check-in/reset/silence, cross-tab sync — none edited); the one test that pinned the old behavior was rewritten to assert no-fire/idle/flag-set, and its doc comment now records that the founder call was made and why rather than dropping the context. 8 net-new tests in that file (both `startedAt` shapes, non-positive duration, the structured log + reason, the persisted repair, dismiss, cleared-by-`start()`, StrictMode double-invoke, and an explicit "a genuinely expired backgrounded timer still fires" guard against over-correcting). Suite **317 passing** (309 before); `tsc --noEmit`, lint, `npm run build` all clean.

- [x] `T21.20` **Replaced reef-band input with cross-shore transect sampling — the banding logic was right, the input was wrong** (`src/lib/sites/reef-bands.ts`, `reef-bands.test.ts`, new `src/lib/sites/urm-client.ts` + test). `deriveReefBands()` was fed polygon **vertices** from Florida's Unified Reef Map. Vertices trace polygon *boundaries*, so one reef polygon running miles along-shore emits a sample at nearly every distance from the entry and fills the very sand gaps the algorithm splits on: Datura Ave collapsed to a single 149-1771 yd band and Vistamar to 129-1891 yd, while South Beach's apparently-correct 3 bands were luck of the data (that area happens to be mapped as several separate polygons), not method. **New `sampleTransect(entry, polygons, {bearingDegrees=90, maxYards=2000, stepYards=25})`** walks straight offshore and asks *which polygon contains this point* at each step — how a diver actually experiences the profile, and exact rather than incidental. 81 point-in-polygon tests against polygons already in memory; no spatial index. `deriveReefBands()` and all 12 of its existing tests are **unchanged**. **Ring handling is the load-bearing detail:** ArcGIS returns the outer boundary first and holes as later rings, so the tempting "inside any ring" test reports reef for a point sitting in an enclosed sand channel — `isPointInPolygon` counts ray crossings over *all* rings together (even-odd), so a point in a hole crosses twice and correctly reads as outside. Not hypothetical: on live data the naive test differs at 4/81 transect points off Datura, 4/81 off South Beach, 9/81 off Vistamar. **`urm-client.ts` is the I/O boundary** — POSTs to `gis.myfwc.com/.../Unified_Florida_Reef_Tract_Benthic_Habitat/MapServer/16/query` (unauthenticated public FWC service, no key, no cost), point+distance spatial filter in `esriSRUnit_Meter`, `outFields=ClassLv1`, `outSR=4326`, `geometryPrecision=6` (~0.11 m — far finer than the 25 yd step, and it halves the payload from 2.5 MB to 1.4 MB, measured live). Never throws: network failure, 20 s timeout, non-2xx, and **an ArcGIS error delivered inside an HTTP 200 body** (their convention — `response.ok` alone would read it as "zero reef here") all return `{polygons: [], error}` with a structured `logger` call. `exceededTransferLimit` is surfaced as an additive `truncated: boolean` rather than swallowed, because a dropped polygon silently becomes a missing reef band, i.e. an under-reported swim distance. Sends a descriptive `User-Agent` for the reason `osm-import.ts` documents at length. **Live results (all three entries, run against the real service):** Vistamar now resolves cleanly into **150-525 / 875-1450 / 1725-1850 yd** (was one 129-1891 band) — the classic Fort Lauderdale three-reef profile. Datura resolves into 175-1200 / 1450-1700 / 1900-1975. South Beach resolves into 1050-1125 / 1725-1975. **Two results that do NOT match the expected ground truth, reported rather than tuned away.** (1) **Datura's first and second reef still merge.** Both founder-confirmed features are present at the right distances — hardbottom starts 175 yd, `Ridge` runs 650-800 yd — but the URM maps the ground between them as near-continuous hardbottom with sand channels only 25-150 yd wide, all narrower than `BAND_GAP_YARDS = 200`. Stable across bearings 75-105, so it's the data, not the transect direction. The previously-recorded "~546 yd gap of sand" was itself a vertex-sampling artifact (two surviving boundary clusters); there is no such gap. What separates LBTS's first and second reef is **depth** (12-20 ft vs 30-50 ft) and this layer carries no depth attribute — lowering the threshold to force a split shatters the outer reefs into six fragments instead, so this needs a bathymetry input and a founder decision, not a threshold tweak. Documented as a Known Limitation in the module header, including that the UI must not present "First Reef, 175-1200 yd" as one homogeneous dive. (2) **South Beach drops from 3 bands to 2** — correctly. Its old 255-435 yd band came from small hardbottom patches lying *off* the straight-out line (they appear on bearings 60 and 105, not 90); a single ray answers "what do I swim over going straight out", not "what is near this entry", and that limitation is documented too. **Corridor filter kept, not removed:** `CORRIDOR_HALF_WIDTH_YARDS` is now inert on the transect path by construction (a transect drifts <1 yd of latitude over 2000 yd at Florida latitudes, so nothing it produces can be rejected), but `deriveReefBands` accepts an arbitrary `HabitatSample[]` and this is its only guard against a caller handing it scattered points — kept and re-documented as a precondition check on untrusted input, explicitly *not* a second banding mechanism. Removing it would also have required deleting an existing test, which this task forbade. **23 new tests** — 15 in `reef-bands.test.ts` (point-in-polygon inside/outside/**in a hole**/outside-a-hole/off the end of a band; transect distance placement, unmapped steps omitted rather than called sand, nothing-in-range, explicit bearing, reef-wins-over-sediment on overlap, non-positive step/range returning empty instead of looping forever; and end-to-end sampling→banding across separate polygons, across a hole in a *single* polygon, a continuous polygon not splitting, plus an explicit regression case proving vertices of an along-shore polygon and its transect give different answers) and 8 in `urm-client.test.ts` (ring preservation, request-shape/User-Agent assertions, degenerate-feature filtering, the HTTP-200 ArcGIS error, `truncated`, non-2xx, network throw, invalid JSON) — all synthetic fixtures, no network. `npx tsc --noEmit`, lint, **463 tests passing**, and `npm run build` all clean. **Not wired up:** nothing calls `fetchReefPolygons`/`sampleTransect` yet; and `shore-access.ts`'s header still repeats the "then a gap, then Aggregate Reef at ~704 yd" description that this pass disproved — that file is owned by another agent right now, so it's flagged, not edited. Its `SHORE_DIVE_MAX_MILES = 0.5` is unaffected (the ~704 yd second reef is still there).

- [x] `T21.21` **Schema for multi-catalogue aggregation — depth, shore access, and multi-source provenance** (`supabase/migrations/0012_sites_dive_metadata.sql`, `supabase/README.md`, `src/lib/sites/types.ts`, `queries.ts`, `queries.test.ts`). `sites` was shaped around one upstream catalogue (`0008`) and around what a map pin needs (`0010`); aggregating OpenStreetMap + FWC artificial reefs + FWC Unified Reef Map breaks both. Three additions, each persisting a module that already existed and was tested (`dive-suitability.ts`, `shore-access.ts`, `dedupe.ts`) — the schema was written to match them, not the reverse. **(1) Depth** — `depth_min_ft`/`depth_max_ft`, nullable `numeric(5,1)`, with `sites_depth_min_le_max` and a non-negative CHECK. **Feet is in the column names, not just a comment**: a 30 m wreck read as 30 ft would present a ~100 ft AOW dive as an Open Water one, and feet is already the unit of both the source data and `dive-suitability.ts`'s limits. Nullable is load-bearing — most catalogue records genuinely have no depth, and `classifyDiveSuitability()` deliberately answers "Depth not recorded" rather than inventing a level *or* treating unknown as too-deep; `NOT NULL DEFAULT 0` would turn "we don't know" into a depth claim. `min` is the *shallowest divable* depth (a wreck's deck), which is what suitability classifies on. **(2) Shore access** — a `shore_access_confidence` enum (`likely`/`marginal`/`unlikely`, matching `ShoreAccessConfidence` exactly; no `confirmed` value, ever, since distance is the only input) plus `shore_access`, `shore_entry_id` and `shore_distance_yards` (**yards**, converted from the classifier's miles at the write). NULL is a third real state — "not yet classified", distinct from `unlikely`, which is itself also the honest answer for a site reachable from an entry nobody has catalogued yet, so it must never render as "boat access only". Storing the entry and distance is what lets the UI say "295 yd from the 5th Street beach entry" instead of an unexplained badge, and what makes re-classification auditable when the entry list grows or a threshold is corrected (it already was once, 0.25 → 0.5 mi). `shore_entry_id` is text, not an FK — the entry points are a curated constant in code, so a stale slug degrades to showing the distance alone, never to inventing a name. **(3) `site_sources`** — one row per (site, catalogue, upstream id), unique on `(external_source, external_id)` and deliberately **not** on `(site_id, ...)`: one upstream record describes one physical site, so including `site_id` would permit exactly the duplicate deduplication exists to prevent. Both columns NOT NULL, so unlike `0008`'s partial index this is a plain unique index usable as an `ON CONFLICT` target. **RLS follows `0005_webcam_readings.sql`** (public read, service-role-only write) rather than `0009`'s no-policy-at-all posture — `site_sources` describes public-read `sites` and carries the ODbL attribution the UI must render, whereas `0009` is internal rate-limit bookkeeping. **`0008`'s columns are redundant but NOT dropped here** — `osm-import.ts` writes them and its duplicate pre-check depends on `sites_external_source_id_idx`; documented as `site_sources` = authoritative / `0008` = denormalized "originating source", with a three-step backfill-then-drop plan tracked as README Open item #16 rather than left as a silent second source of truth. **Types/queries wired:** all five fields on `Site`; only `depth_min_ft`/`depth_max_ft`/`shore_access` on `SiteMarker` (fetched for every pin in view — depth and shore access drive map-level *filtering*, per `dive-suitability.ts`'s own "the signal to exclude it from a recreational map"; the entry-id/distance pair is one-line prose for a single opened site and would be pure bloat across hundreds of pins). Every new field optional *and* nullable so consumers under `src/app/**`/`src/components/**` compile untouched. Both column-list constants updated, plus a new `toNullableNumber()` for the `numeric`-as-string columns — **a new test caught a real bug in it**: `Number("")` is `0`, so an empty depth field would have arrived as a literal "0 ft" claim, the worst possible wrong answer since 0 reads as a real, extremely shallow depth rather than missing data. Unparseable/empty now degrades to `null`, never `NaN` (which would sail past every comparison in `classifyDiveSuitability()` and yield a confident "within recreational limits" for an unknown-depth site). The two duplicated marker-mapping literals were also collapsed into one `normalizeMarker()` — the mapping counterpart of `SITE_MARKER_COLUMNS`, which existed to stop exactly that drift one layer up. **7 net-new tests** (exact marker column list asserted in both queries, detail-only columns kept off the pin list, numeric-string coercion, null-preservation, the empty-string/NaN guard, and the detail read's shore-access explanation fields); suite green with no regressions (**408 passing** when this pass ran, 455 on a re-run minutes later — the absolute count moves while parallel agents land work, so "no failures" is the meaningful check, not the number), `tsc --noEmit`, lint, `npm run build` all clean. **⚠️ The migration has NOT been applied** — no DB credentials in this environment; founder applies it manually via the Supabase SQL editor, same as every other file in `supabase/migrations/`. Until then `queries.ts` will fail loudly against the un-migrated project (which is the point of the explicit column lists). Tracked as README Open item #15 with a smoke test; #17 flags that the new derived columns are self-service-writable via `sites_insert_own`, same as `description`.
- [x] `T21.22` **Site detail page enriched for the diver the founder actually described** ("I want to know of all dive sites as I may end up on a boat charter going there. Or specifically target one that does") — `src/app/sites/[id]/page.tsx` plus three new components (`site-location-map.tsx`, `site-dive-profile.tsx`, `site-sources.tsx`) and `src/lib/sites/site-depth.ts`. Everything already on the page is preserved; this is enrichment, not a rewrite. **(1) Location map.** The page had none. `SiteLocationMap` renders one site by **importing `src/lib/sites/pin-icons.ts`** (`drawPinIcon` → `map.addImage` under `pinIconName(spec)` → symbol `Layer` with a data-driven `["get","icon"]` expression) — the same three steps `site-map.tsx` uses, so a site is byte-identical between its detail page and the exploration map, and the upcoming pin design pass (Resolved Decision #7) still has exactly one copy of the artwork to change. The one difference: `site-map.tsx` registers all 72 specs because any site can enter its viewport; this one knows which site it is drawing and registers one. Unknown `site_type` falls back to `unclassified` — an unregistered image renders as *nothing*, i.e. a site invisible on its own map. **Lightly interactive, not static:** pan/pinch/keyboard/`NavigationControl` on; **scroll-zoom off** (the map sits inside a scrolling article and would otherwise trap a phone mid-page — the zoom buttons exist so turning it off doesn't remove zoom), rotation/pitch off. Degrades to a placeholder without `NEXT_PUBLIC_MAPBOX_TOKEN` **that still states the coordinates**, so the page never loses the thing the map was there to convey. Accessibility mirrors `site-map.tsx`'s visually-hidden-list remedy for a symbol layer having no DOM node: an `sr-only` description carrying name, exact coordinates, provenance, hazard and legal-access status. No hydration hazard — nothing branches an initial `useState` on `typeof window`/`navigator` (the bug documented in `use-geolocation.ts`) and `initialViewState` comes from server props. **(2) Depth/certification + shore access.** Rendered from `dive-suitability.ts`/`shore-access.ts`, whose headers define what may and may not be claimed; those constraints are now visible copy and are pinned by tests. Shore access is *never* confirmed ("Plausibly shore-accessible — verify conditions"), the `marginal` tier reads as a real **20–30 minute surface swim each way, not a stroll off the beach**, and `unlikely` explicitly says it is **not** a finding that the site is boat-only (an uncatalogued entry produces the identical result). Certification is guidance, never permission ("Within Open Water training depth"); unknown depth renders "Depth not recorded" plus "missing data, not a sign the site is shallow", never a level. `FLORIDA_DIVER_DOWN_FLAG_NOTICE` is surfaced as a **Legal requirement** callout for every shore-accessible site. A technical-depth site is labelled plainly ("Technical dive — beyond recreational limits") and explained rather than hidden — it is legitimate reading material for someone deciding whether to pursue the training. **One real correctness catch:** `CERTIFICATION_LABEL` is *not* used verbatim for the 100–130 ft band, where `minimumLevel` is already `beyond_recreational` while `entirelyBeyondRecreational` is still false — printing "Beyond recreational limits" there would wrongly place a still-recreational depth outside the limit; that band gets its own "Deeper than Advanced Open Water training depth" + deep-specialty note. **(3) Depth resolution.** `0012`'s `depth_min_ft`/`depth_max_ft` are **not applied to the live DB** and aren't in `SITE_DETAIL_COLUMNS`, so `resolveSiteDepthFt()` reads them defensively (optional, possibly numeric-as-string, inverted pairs re-ordered, `0`/`NaN`/negative rejected — a bogus 0 ft would silently classify a site as Open Water) and otherwise falls back to the `"Reported depth: …"` line `osm-import.ts` writes into `description`, anchored to a line start so it parses **our own generated output and never the source's prose** (real OSM text says "rests … in 25 feet of water about 150 yards offshore" — mining that would fabricate a certification judgement). Source is part of the contract: a description-sourced depth is disclosed in the UI as reported-and-unreviewed, with the raw wording shown. **(4) Sources.** `description` now renders as paragraphs instead of one collapsed `<p>`, which is what makes the ODbL attribution `osm-import.ts` embeds actually readable; a "Where this data came from" section restates it as standalone attribution with licence links, says plainly that `COMMUNITY` means nobody has checked this entry against the water, and separates **this app's derived** shore-access/depth guidance from the source's own claims so our inference can't borrow the source's credibility. Also surfaced `SiteDetailResult.truncated` (built by `T21.16`, previously unrendered) so a capped hazard list can't pass as a complete history. **55 new tests** — `site-depth.test.ts` (20, node) plus three jsdom suites deliberately written as *copy* tests, because a rewording that turns "plausibly shore-accessible" into "shore dive" breaks nothing a normal test notices: the negative assertions (no "you can dive", no "shore dive: confirmed", `likely` and `marginal` not rendering identically) carry more weight than the positive ones. `drawPinIcon` stays untested for the reason `pin-icons.test.ts` already documents (needs a real Canvas 2D context); the property that matters — this component resolving the *same* `pinIconName` `buildSiteFeatureCollection` would — is asserted directly instead. Suite **463 passing** (378 at the start of this batch; +55 mine, rest from parallel agents); `tsc --noEmit`, lint, `npm run build` all clean. **Not verified visually** — no live Mapbox token in this environment, so the rendered map is unconfirmed; the no-token placeholder path is the one covered by tests.
- [x] `T21.23` **Dive difficulty rating — beginner/intermediate/advanced/technical, composed from what already existed** (`src/lib/sites/dive-difficulty.ts` + test, wired into `site-dive-profile.tsx` as a new "Dive difficulty" section, plus a structured overhead-environment callout that `water-access.ts` had defined since it was written but nothing had rendered until now). Founder ask: "Add risk scores and state the type of dive.. beginner, intermediate, advanced etc." Deliberately a thin composition layer over `dive-suitability.ts` (depth), `water-access.ts` (overhead environment) and `shore-access.ts` (swim distance) rather than a fourth independent judgement — each of those is already correct and tested in isolation, and the real risk in this feature is combining them wrong, not any one of them being wrong. **Cave overrides everything, unconditionally.** A 20 ft cave entrance returns `technical`/risk 5 regardless of depth or swim distance — averaging it in with depth would have let a shallow, easy-entry cave read as "medium risk," which is exactly backwards; easy entry is what makes an overhead environment dangerous, not what makes it safe. Depth alone maps to beginner/intermediate/advanced/technical via the existing certification bands; cavern presence and a `marginal`-tier shore swim each add one point to a capped 0-5 score. **`riskFactors: string[]` is not optional decoration** — `classifyDiveDifficulty`'s own header and a dedicated test (`never returns an empty factor list alongside a real level`) require every non-null `level` to carry the measurements that produced it, so the UI can never show a bare "Advanced" badge with no reasoning attached. Unknown depth AND no overhead signal returns `level: null` (not a guessed default) with an honest "not enough data" message — same discipline as `dive-suitability.ts`'s own unknown-depth handling. **Promoted a real, previously-invisible gap while wiring this in:** `water-access.ts` has carried a cave/cavern `overheadWarning` since earlier this session, but no component ever rendered it structurally — Eagle's Nest's warning existed only as prose buried inside its imported `description` text. It's now a `role="alert"` red callout on the detail page, matching the diver-down flag's existing amber-callout treatment, and verified live against the real imported row. **Two bugs caught by the tests, not by review:** an initial test set expected a 95 ft wreck to land in the deep-specialty band, which the code correctly refused (95 ft is ≤100 ft, i.e. still standard Advanced Open Water) — the test's depth was wrong, not the classifier; and a shore-swim test fixture was placed 1249 yd from its entry, past `SHORE_DIVE_MAX_MILES` (880 yd), so it silently landed in `unlikely` instead of the intended `marginal` — recomputed from the precise degrees-per-yard constant this session already derived once for `reef-bands.test.ts`. **18 new tests** (13 in `dive-difficulty.test.ts`, all anchored to real depths/coordinates already in the catalogue: Copenhagen-style 15-30 ft, Caicos-Express-style 190-240 ft, the real Datura first/second reef entries; 5 new in `site-dive-profile.test.tsx` for the badge, the no-data case, and the cave/cavern callout — plus all 19 pre-existing component tests re-verified unchanged). `site_type` made optional on `SiteDiveProfileProps` rather than required, so none of the 15 existing test fixtures needed touching for an unrelated reason. Suite **493 passing** (was 465 at the *T21.22* landing point — 15 net-new plus whatever moved in between); `tsc --noEmit`, lint, `npm run build` all clean.
- [x] `T21.24` **Depth-aware reef-band splitting — solves the Datura Ave "Known Limitation" `reef-bands.ts` left open at T21.20, via a new NOAA bathymetry client** (new `src/lib/sites/bathymetry-client.ts` + test, additive changes to `src/lib/sites/reef-bands.ts` + `reef-bands.test.ts`). T21.20 proved live that the Unified Reef Map maps the ground between Datura's first and second reef as near-continuous hardbottom — sand channels only 25-150 yd wide, all narrower than `BAND_GAP_YARDS` — and documented that no distance/habitat threshold could split them; depth was the missing input, deferred as a founder decision. Founder asked for it to be solved this session. **NOAA publishes free, keyless bathymetry that resolves it.** `bag_bathymetry` (BAG multibeam mosaics, ArcGIS ImageServer, no API key/quota/cost) and `DEM_mosaics/DEM_all` (CUDEM, ~3.4 m/pixel, same request family) verified live at 26.17,-80.095 (between Datura's two reefs): -4.78 m and -5.48 m respectively — real coverage, no budget impact (CLAUDE.md's Funding & Cost Model). **`bathymetry-client.ts`** mirrors `urm-client.ts`'s conventions exactly (descriptive `User-Agent`, `AbortController` timeout, structured `logger` calls, the ArcGIS-error-in-an-HTTP-200-body trap, never throws): `fetchDepthFt(point)` for a single point, `fetchDepthsFt(points)` for a batch via one `getSamples` call rather than N sequential round-trips, and `sampleDepthTransect(entry, options)` as the depth counterpart of `reef-bands.ts`'s `sampleTransect`. BAG is tried first; a **whole-request** failure (network/timeout/non-2xx/ArcGIS error) retries once against CUDEM; in a batch, individual points BAG reports `NoData` for (a real coverage gap, not a failure) are backfilled with a second, restricted CUDEM `getSamples` call rather than discarding an otherwise-good transect. **Sign/unit conversion is the part a bug here would silently corrupt a certification judgement**: both services return negative metres relative to MLLW; every value goes through exactly one `Math.abs(metres) * 3.28084` conversion, and a reading at or above the datum (>= 0 m) is deliberately converted to `null` rather than `Math.abs()`-ed into a false depth — a caught real design point, since every caller queries points already offshore, so a non-negative reading means dry land/an exposed flat, not water. Pinned by a test against the live-verified `-4.77755 m -> ~15.67 ft` figure, not an arbitrary fixture. **A genuine bug the tests caught**: an early version of `fetchDepthFt`/`fetchDepthsFt` reported only the fallback service's error message when both BAG and CUDEM genuinely failed, silently hiding that the primary had also failed rather than merely had a coverage gap — fixed to combine both messages (`"BAG: ...; DEM fallback: ..."`) when both are real failures, caught by an assertion that checked for both status codes in the combined string. **`deriveReefBandsWithDepth`** (additive, new — `deriveReefBands` and its exported behavior are untouched, since a parallel agent, T21.25, was building an import pipeline against that exact interface with zero visibility into this change) adds a second pass after the existing sand-gap split: within a distance-merged group, split again wherever consecutive-by-distance samples' depth increases by more than `DEPTH_STEP_FT`. Deliberately a **separate, duplicated implementation**, not a refactor of `deriveReefBands` to share the grouping logic — the ~40 lines of duplication is the accepted cost of guaranteeing zero risk of colliding with concurrent edits to the function another agent's work depends on. New `DepthSample` type declared in `reef-bands.ts` (not `bathymetry-client.ts`), matching the existing `ReefPolygon`/`urm-client.ts` split so the pure half of this feature still doesn't depend on the I/O half; matched to habitat samples by recomputing each one's distance-from-entry and rounding to the nearest yard (both transects place points via the identical spherical formula, so this lands exactly). New optional `ReefBand.depthFt?: DepthRangeFt` field, `undefined` for every existing caller since only the new function populates it — confirmed by an explicit regression test asserting `deriveReefBands`'s own output never sets it. **`DEPTH_STEP_FT = 7` (ft) chosen the same empirical way `BAND_GAP_YARDS` was**, from live NOAA data across all three known entries: comfortably above the largest false-positive single-step increase found *inside* an already-correct band (Vistamar's second band, +6.2 ft at 1025-1050 yd) and comfortably below the real Datura break (+8.6 ft on the default bearing 90, +9.0 ft on bearing 75, +10.3 ft on bearing 105 — all landing at the same 800-825/850 yd mark). **Live verification, real numbers, not estimates:** Datura's depth profile rises to ~15-19 ft through 175-800 yd (matching the founder's 12-20 ft first reef), jumps to 20.4-23.0 ft over the single 800->825 yd step, and continues deepening to 29.6-41.1 ft by 1200 yd (matching the founder's "30-50 ft" second reef — shallow side of that range near its inshore edge, but the right regime) — and that depth step lands exactly where the habitat profile (T21.20) changes from `Ridge` to patch/`Aggregate Reef` at the same 825 yd mark, two independent datasets corroborating one boundary rather than depth inventing one alone. Result: **Datura's previously-merged 175-1200 yd band now splits into 175-800 yd (9.6-19.3 ft) and 825-1200 yd (20.4-41.1 ft)** — the founder-confirmed two-reef split, achieved. **Cross-check held**: Vistamar's three already-correct bands (150-525 / 875-1450 / 1725-1850 yd) and South Beach's two (1050-1125 / 1725-1975 yd) came through this pass **unchanged** — no spurious over-splitting. One extra, unprompted finding, disclosed rather than acted on: Datura's third band (1450-1975 yd, already separated by a sand gap) also splits under this same pass into 1450-1675 yd (42.0-54.0 ft) and 1700-1975 yd (69.1-72.3 ft) — a real, steep drop-off the habitat/distance-only method had no way to see, but *not* founder-confirmed ground truth the way the first/second reef split is, so it's documented in `reef-bands.ts`'s header as a genuine finding worth field-checking, not presented as an established fact. **25 new tests** (8 in `reef-bands.test.ts`: the Datura split reproduced with dense 25 yd-step synthetic samples shaped after the live profile, the exact-threshold boundary, missing-depth-never-triggers-a-split, distance-based rather than array-position matching, the corridor filter still applying, a custom `depthStepFt` override, empty-input parity with `deriveReefBands`, and the `deriveReefBands`-never-sets-`depthFt` regression guard; 17 in `bathymetry-client.test.ts`: the sign/unit conversion, `NoData` handling, the land/positive-datum null rule, the BAG->DEM single-point and batch fallback paths, the combined-both-failed error message, the ArcGIS-200-body-error trap, network-throw handling, the User-Agent/request-shape assertions, per-point `NoData` backfill restricted to only the gap points, backfill failure degrading gracefully instead of failing the whole batch, and `sampleDepthTransect`'s stepping/never-omits-a-step/non-positive-range guards). Suite **544 passing** (was 519 at T21.25's landing point; +25 net-new, no regressions — `deriveReefBands`'s original tests all still pass unmodified). `npx tsc --noEmit`, lint, `npm run build` all clean. Live-data verification (transect sampling across all three entries, bearing-stability check at 75/90/105, threshold sensitivity analysis) was run from a throwaway scratchpad script against the real NOAA/FWC services, then deleted — not committed, per this task's own instructions. **Not wired up**: nothing under `src/app/**`/`src/components/**` calls `deriveReefBandsWithDepth`/`bathymetry-client.ts` yet, and T21.25's `reef-band-sites.ts` (built in parallel, before this landed) still only special-cases Datura's depth by band-count rather than reading a real `depthFt` field — flagged as follow-up, not fixed here, since that file belongs to that task.
- [x] `T21.26` **Dive difficulty usable while browsing, not just on the site detail page — filter chip row + list badge** (`src/lib/sites/explorer-preferences.ts` + test, `src/components/dive-site-explorer.tsx`, `src/components/nearby-dive-sites-list.tsx`). Founder ask: T21.23's `classifyDiveDifficulty()` only rendered on `site-dive-profile.tsx`; a diver scanning the map/list had no way to filter or scan by difficulty without opening every site. **Entirely client-side, no new query or migration** — `SiteMarker` (`SITE_MARKER_COLUMNS`, T21.21) already carries `latitude`/`longitude`/`depth_min_ft`/`depth_max_ft`/`site_type` for every pin in view, exactly `classifyDiveDifficulty`'s three inputs, so a new `siteDifficultyLevel(site: SiteMarker)` helper (exported from `dive-site-explorer.tsx`, imported by the list component so filter and badge can never independently mis-derive the same value) just calls it directly per site. **Filter**: a `DifficultyFilterRow` chip row (All / Beginner / Intermediate / Advanced / Technical), same native-button style as the existing `SiteTypeFilterRow`, applied at the `DiveSiteExplorer` level so map pins and the list stay in sync (identical reasoning to T21.6's radius fix and the 2026-08-09 type filter — a filter that only touched the list would leave stale pins on the map). New `difficultyFilter` field added to `explorer-preferences.ts`'s existing persisted store, mirroring `siteTypeFilter` field-for-field: `"all"` default/sentinel, a hand-maintained `DIFFICULTY_FILTER_VALUES` runtime array validated on every read (a persisted value from an older build or hand-edited storage falls back to `"all"`, never trusted blind), round-trip + corrupt-storage + cross-tab tests added alongside the existing ones. **Explicit decision on `level: null` sites** (classifier's own "not enough measured depth/overhead data" state, per its header a real absence-of-data signal, never a default): they show only under "All" and are excluded from all four named filters — falls out of a plain equality check (`null` never equals a filter string) rather than a special case, and documented as deliberate in `dive-site-explorer.tsx` so it isn't mistaken for an oversight later. **Badge**: `DifficultyBadge` in `nearby-dive-sites-list.tsx`'s `SiteRows`, reusing `DIFFICULTY_LABEL` text, rendering nothing (not a placeholder) when `level` is `null` — CLAUDE.md's "never imply data you don't have" rule applied literally, since unlike `LegalAccessBadge`'s `unassessed` state an unrated difficulty has no claim worth surfacing at all. **Badge colors deliberately break from `site-dive-profile.tsx`'s own emerald/blue/amber/red scheme**: that page never renders `LegalAccessBadge` next to the difficulty badge, this list always does (`ProvenanceBadge` + `LegalAccessBadge` sit immediately adjacent in the same row), and emerald/amber are already load-bearing there for "open access"/"caution" — reusing them for difficulty would make a difficulty badge misreadable as a legal-status claim. Chose teal/blue/orange/red instead: each a distinct Tailwind scale from every hue already on that row (teal≠emerald, blue≠sky, orange≠amber, red≠rose) while still reading as an intuitive low→high risk ramp on its own. Tooltip text on the badge and the "All" chip both repeat `dive-difficulty.ts`'s own disclaimer verbatim ("not a safety rating") rather than a softened rewrite, per that module's header on what may/may not be claimed. Empty-state copy in the list also updated to name *both* active filters together ("No beginner reef sites within 25 mi…") instead of silently dropping one when both a type and a difficulty filter are active. **Pin visuals untouched** — `site-map.tsx`/`pin-icons.ts` not opened; difficulty narrows which markers `mapSiteMarkers` includes, never a 5th pin-color dimension, per this task's explicit scope boundary. **7 new/updated tests** in `explorer-preferences.test.ts` (default value, round-trip, unrecognized-value fallback, and the three existing multi-field tests extended to cover `difficultyFilter` too). Suite **519 passing** (was 493 at T21.23's landing point — some of the delta is concurrent parallel-agent work landing in the same window, not all mine); `tsc --noEmit`, lint, `npm run build` all clean. **Not verified visually** — no live-rendering check in this environment; correctness and copy honesty were the verification bar per this task's own instructions.
- [x] `T21.27` **Pin-tap preview sheet, and removed dev-scaffolding from the production homepage — the first two items of a founder-directed UX pass** (`src/components/site-pin-preview-sheet.tsx` + test, `src/components/site-map.tsx`, `src/app/page.tsx`). Founder ask: look at the app "through the lens of a creative director, scuba diver, dive shop owner, diving school/dive master, and scuba tour guide" — it wasn't as useful/intuitive as hoped. **Finding #1:** `creative/mockups/map-exploration/03-pin-preview-sheet.html` designed a compact bottom-sheet preview for a pin tap from the start, but the shipped `site-map.tsx` instead did `router.push` straight to the full detail page on every tap — no intermediate step, ever. That's a real cost for exactly the personas named: a dive master or shop owner scanning several pins to plan a group outing had to leave the map and navigate back for every single site, on a page that otherwise goes out of its way to preserve map state across navigation (`explorer-preferences.ts`). **Built the originally-designed interaction with real data**: `SitePinPreviewSheet` shows name, site-type label, `ProvenanceBadge`/`LegalAccessBadge` (reused, not reinvented, so the sheet and full detail page can never disagree about what a badge means), and — since `SiteMarker` already carries `depth_min_ft`/`depth_max_ft`/`shore_access` per `T21.21` — a depth stat, an honest shore-access label (`"Plausibly shore-accessible"`, never "confirmed," matching `shore-access.ts`'s own rule), and a hazard-reported flag, each rendered only when the underlying data exists. `site-map.tsx`'s click handler now opens the sheet (`setPreviewSiteId`) instead of navigating; `useRouter` was removed from the file entirely since nothing else used it. **Finding #2, found while reading the homepage for the audit:** `src/app/page.tsx` permanently rendered an honestly-labeled but purposeless "Example: cached site entry" block — `MOCK_CACHED_SITES`, two placeholder rows literally named `La Jolla Cove`/`Shaw's Cove` (the original California seed data), demoing `ProvenanceBadge`+`FreshnessBadge` together. It was T13.3 scaffolding that never got a demo route (unlike `/media-demo`) and was left on the real, live homepage — directly under a now-real map of 431 South Florida sites, contradicting CLAUDE.md's own "avoid noisy... low-noise product" principle, and doubly obsolete since a **real** per-site offline/prefetch section (`PrefetchButton`, `T12.6`/`T12.7`) already exists on the site detail page. Removed outright rather than gated behind a flag — nothing on the real homepage needs it, and the real feature it was standing in for already shipped elsewhere. **7 new tests** for `SitePinPreviewSheet` in isolation (name+link target, close callback, depth formatting including the equal-min/max single-value case, depth omitted when unrecorded rather than shown as blank/zero, hazard flag shown only when true, shore-access label never claims "confirmed") — `site-map.tsx` itself stays deliberately untested for the same documented reason `pin-icons.test.ts` gives (no real Canvas/WebGL context in this codebase's test environment; nothing here invents a new testing approach that precedent avoided elsewhere). Suite **519 passing**, `tsc --noEmit`, lint, `npm run build` all clean — verified both in isolation and after a parallel agent (`T21.26`, the difficulty filter) landed in the same session, confirming no collision (that agent was explicitly scoped away from `site-map.tsx`/`pin-icons.ts`, confirmed by both agents' independent reports). **Rest of the UX pass — audited, not yet built**: see the founder-facing summary for the map-degraded-fallback gap (`creative/flows/map-exploration.md`'s scrollable-card list, already flagged in `page.tsx`'s own comments as separately out of scope), the four other unimplemented `creative/mockups/` flows (`onboarding`, `voice-logging`, `offline-prefetch`, `lds-moderation`), and the open question of what a dive-shop-owner/dive-master/tour-guide-specific view would actually need (multi-site trip planning, a printable/shareable site brief, a "recommend to a customer" path) — none of that is product-scope-confirmed yet, so nothing was built toward it without asking first.
- [x] `T21.25` **Reef bands → candidate `sites` rows, dry-run only — the schema-shaping half of the reef-band pipeline** (`src/lib/sites/reef-band-sites.ts` + test). `deriveReefBands()` (T21.20) answers "what reef lines sit offshore of this entry"; this is pure, unit-tested `buildReefBandSiteCandidates(entry, bands)` answering "what would a `sites` row for each of those lines look like" — no network, no database, safe to call speculatively. Every candidate is `shore_reef`/`COMMUNITY`, never `VERIFIED` (this is automated inference, not a human vouching for a row — same reasoning `osm-import.ts` and `reef-bands.ts`'s own header already apply), `legal_access_status: null` (URM carries no such data), and a `description` that explicitly discloses the derivation — cross-shore transect sampling from a named entry, "nobody has dived or verified this specific derived boundary" — following `osm-import.ts`'s attribution/disclosure convention. **Depth: only the documented Datura exception, nothing invented.** `depth_min_ft`/`depth_max_ft` stay `null` for every entry except Datura Avenue, and even there only when `deriveReefBands` hands back *exactly two* Datura bands (the founder-confirmed first/second reef split, 12-20 ft / 30-50 ft per `shore-access.ts`'s cited published guidance) — a merged single band (today's actual live behavior, per `reef-bands.ts`'s own "Known limitation") or an unexpected 3+ band split both fall through to `null` rather than guessing which band a documented depth belongs to. The `ReefBand` interface this module was built against carries no depth field of its own (a parallel bathymetry effort was in flight but hadn't landed one as of this task), so nothing here reads a speculative field name — `resolveDepthFt()` is called out as the one place to wire one in later. **Shore access reused, not reimplemented:** `classifyShoreAccess(band.arrivalPoint, [entry])`, deliberately scoped to *only* the originating entry so a band's nearest-entry attribution can never resolve to a different, closer-by-coincidence entry than the one it was actually derived from — a real failure mode named directly in the module header and covered by a regression test (Vistamar bands must never attribute to nearby Datura). A band beyond `SHORE_DIVE_MAX_MILES` still produces a candidate, honestly tagged `unlikely`, rather than being silently dropped. **17 new tests** in `reef-band-sites.test.ts` covering: empty-bands passthrough, ordinal/naming convention, COMMUNITY/shore_reef/null-legal-status invariants, arrival-point placement, disclosure-language assertions (including a negative check against "shore dive: confirmed"), entry-note inclusion/omission, all three Datura-depth cases (exactly-two-bands assigns depths, one merged band does NOT force a split, an unexpected three-band split also doesn't guess), all three shore-access tiers, the far-band-still-produces-a-candidate case, and the entry-attribution regression guard. **Scratch dry-run runner** (`/private/tmp/.../scratchpad/dry-run-reef-band-sites.ts`, not committed) fetches live URM data for all 8 `SOUTH_FLORIDA_ENTRY_POINTS`, derives bands, builds candidates, and dedupes against the live `sites` table (431 rows, read-only `SUPABASE_SERVICE_ROLE_KEY` query — no `.insert()`/`.upsert()` anywhere in the file) via `dedupe.ts`'s `areSameSite`. **Run live this session: 12 candidates derived across 5 of the 8 entries** (Phil Foster, Mizell-Johnson, Hollywood North Beach returned zero URM polygons in range — no reef bands, not an error), **0 flagged as duplicates** — spot-checked by hand against the real near-miss cases the task called out (`Reefline - "Traffic Jam"` and the `NOVA`/`Swiss Cheese` rows): both sit far enough from where URM's mapped hardbottom actually starts off those entries (>0.25 mi, or within it but with zero name-token overlap) that `areSameSite`'s two-tier predicate correctly calls them distinct, not a dedupe miss. Datura still returns 3 bands live (175-1200 / 1450-1700 / 1900-1975 yd — the bathymetry split hadn't landed as of this run), so none of the 12 candidates carry a depth. Full real output — every candidate's name/coordinates/band/habitat/shore-access/description — is in this task's own report, not summarized away. `npx tsc --noEmit`, lint, **519 tests passing** (no regression), `npm run build` all clean — one transient failure seen mid-session in `dive-site-explorer.tsx`/`nearby-dive-sites-list.tsx` (`difficultyFilter`/`DifficultyFilterRow` not yet defined) was a parallel agent's T21.26 mid-flight, not this task's code; resolved on its own once T21.26 landed, confirmed by re-running clean afterward. **No live database write performed or attempted** — this task was scoped dry-run-only; a human reviews the real output above before any `--write` path is even written.
- [x] `T21.33` **Full-catalogue shore-access audit — 2 real missing entries found, 5 sites reclassified, most flagged "gaps" were false positives** (`src/lib/sites/shore-access.ts`, `shore-access.test.ts`). Founder: verified a real shore dive (S.S. Inchulva / Delray Wreck) via an independent Gemini check, which our app called `unlikely` — then asked to audit the whole catalogue for the same class of gap, not just patch one site. **Root cause confirmed, not a bug**: nearest catalogued entry was Red Reef Park, Boca Raton, 10,913 yd (6.2 mi) away — the correct "under-classify, don't guess" outcome for a genuine coverage gap in the hand-curated entry list, exactly as `shore-access.ts`'s own header predicts. **Two new entries added, both independently researched and cross-checked, same rigor as every existing entry** (real coastline coordinate, not a geocoded business/street address — the recurring lesson from Vistamar/5th Street this session): (1) **Delray Municipal Beach** — two independent sources (the wreck's own OSM description and separate web research) agree on ~150 yd / 15-25 ft; a third, independently measured figure (real Atlantic coastline at the wreck's latitude) put it at 258 yd. (2) **Ocean Inlet Park, Boynton Beach** — found while auditing, not reported by the founder: a real, documented public shore-dive/snorkel jetty ("shore accessible... jetty stretches roughly 100 yards... reef starting in and around the park's jetty... 11-15 ft") with zero entries anywhere near it, serving four FWC-imported "Boynton Inlet"-named sites that were all `unlikely`. **Full 445-site reclassification with both entries live: 5 genuine changes** (Delray Wreck, Boynton Inlet Mitigation Site, Boynton Inlet Reef, Boynton Inlet Stepping Stones Reef, and Goggle-Eye Reef — the last two entries not researched individually but correctly swept up by measuring the real, verified entry point) — shore-accessible count **27 → 32** (23 `likely` + 9 `marginal`). One real negative case correctly stayed unchanged, worth recording since it validates the threshold rather than just adding entries: **SS Copenhagen Shipwreck**, the next-closest real named site to an entry (2,407 yd), researched and confirmed **0.75 mi (1,320 yd) — "boat-access-only... though some intrepid beach divers swim out"** per a real dive operator's own description. That's genuinely past `SHORE_DIVE_MAX_MILES` (0.5 mi), and the "some intrepid divers" framing is exactly what a `marginal`-or-beyond classification should mean — not every audit hit needs a new entry; sometimes the existing threshold is already right. **Two false-positive classes caught in my own audit script before they became bad writes**: (1) every freshwater spring appeared to "regress" from `likely` to `unlikely` — an artifact of naively running the coastal-distance model on site types `water-access.ts` explicitly exempts from it (springs/caves are set at import time, not via `classifyShoreAccess`); excluded from the reclassification pass entirely, not touched. (2) several reef-band-derived sites showed their attributed entry flipping between Datura and El Prado — expected, since those were deliberately scoped to their *originating* entry during derivation (`reef-band-sites.ts`'s own documented reasoning, guarding against exactly this kind of nearer-by-coincidence reattribution); the reclassification pass ignored pure entry-relabeling with no access-tier change. Everything else flagged by the audit (Keys wrecks 50-135 mi away, Spiegel Grove, Vandenberg, other deep boat-only wrecks) was confirmed genuinely far offshore, not a coverage gap. **2 new regression tests** for the two new entries (including a negative case: a real "Boynton Inlet"-named site at 1,425 yd correctly stays boat-access, proving the new entry didn't manufacture blanket access for everything sharing a name prefix). `tsc --noEmit`, lint, **590 passing**, `npm run build` all clean.
- [x] `T21.34` **Second real misclassification found by the founder, root cause was a mis-located entry coordinate, not a missing entry** (`src/lib/sites/shore-access.ts`, `shore-access.test.ts`). Founder web-searched Perry Street Rockpile/Segment III Mit Dania (FWC artificial reef, 18 ft) against two independent sources — diverarchives.com (confirms the reef itself) and scubastar.com, a real dive-tour operator ("the Perry Street Rock Pile begins 600 feet [200 yd] from the shore," naming "Perry St. Beach" as the entry, 12-20 ft) — both agreeing it's a real, run shore dive. The app had it `unlikely` at 3546.5 yd (2 mi). **Different root cause than T21.33's two fixes**: not a missing entry, but the existing `mizell-johnson-dania` entry itself was badly mis-located — ~1300 yd (about 2 mi across, on a barrier peninsula only ~0.4 mi wide) west of the park's real Atlantic beach, onto the Intracoastal side. Exactly the class of bug this module's own header already names (the LBTS Atlantic-vs-Intracoastal mistake) — evidence it's a recurring geocoding failure mode worth checking for elsewhere, not a one-off. **Fixed the existing entry's coordinates** (real OSM `natural=coastline`, easternmost way, at the park's latitude) **and added a new, distinct `perry-street-dania` entry** for the specific, named, operator-documented access point — same precedent as Datura/El Prado being catalogued separately despite being close together. Corrected distance: **76.9 yd** — same order of magnitude as the operator's "600 feet," the gap plausibly explained by FWC's own disclosed caveat that many of its deployment positions are historical/unverified. **Blast radius confirmed narrow, not swept**: re-ran the same full-catalogue `classifyShoreAccess()` audit as T21.33 against all 445 live sites with the corrected coordinates — **exactly 1 change** (Perry Street Rockpile itself); every other site previously attributed to the mis-located entry (Tenneco Towers Deep and 15 others, 2-7 mi out) stayed correctly `unlikely`, confirming the fix targeted the real bug without manufacturing shore access nearby. **2 new regression tests**, including a negative case pinning that a genuinely deep offshore reef in the same cluster (Tenneco Towers Deep) stays unaffected. `tsc --noEmit`, lint, and the full suite all clean; live write applied and verified.
- [x] `T21.35` **Entry-point ground-truthing sweep + new `SHORE_ACCESS_EXCEPTIONS` mechanism, after the founder lost confidence in the data ("I'm now really doubting the integrity of the dive site information")** (`src/lib/sites/shore-access.ts`, `shore-access.test.ts`). Founder's own diagnosis, stated directly and correct: "we should really look at the shoreline closest, not the entry point, as some entry points have also been mis guided" — after T21.34 found `mizell-johnson-dania` ~2 mi mis-located, asked whether other catalogued entries had the same problem. **They did.** Checked every entry's own coordinates against real OSM coastline (or, for the one Intracoastal entry, against OSM's own named dive-spot node) instead of trusting them: (1) **`phil-foster-blue-heron` was ~1.4 mi mis-located** — real coordinates from OSM's "Phil Foster Park" way, cross-checked against OSM's own `Phil Foster Park Snorkel Reef` node (`scuba_diving:divespot=yes`). Fixing it flipped 3 real sites, including **"Phil Foster Park Snorkel Trail" itself** — a site named for this exact park that was showing `unlikely` at 2509.8 yd purely because its own entry was wrong. (2) **`hollywood-north-beach` was ~4.1 mi mis-located**, geocoded well inland; corrected via Nominatim on its real address (3601 N Ocean Dr) snapped to real coastline. No currently-catalogued site sits near enough to flip today, but the entry itself was simply wrong and is now fixed. **Second finding, a different failure mode**: web-searching all 19 sites then flagged as shore-accessible (likely/marginal, excluding springs/caves, which don't use this model) turned up 2 where a credible independent source directly *contradicts* the geometric distance verdict — proving distance-to-entry alone can't detect a hazard between the entry and the site. **Goggle-Eye Reef**: DiveBuddy.com states plainly "you'll need to arrange a boat charter" to dive it specifically. **Peanut Island -NE**: multiple sources agree the island is reachable "only by water taxi, shuttle boat, kayak, or paddleboard" — it sits across the Lake Worth Inlet's navigable channel from the Phil Foster entry, not along open swimmable shoreline. Both were geometrically in range and would have stayed silently wrong under the existing model. **New `SHORE_ACCESS_EXCEPTIONS` list + check in `classifyShoreAccess()`**: a small, citation-required, coordinate-matched override that forces `unlikely` for a known-bad site while still reporting the real nearest-entry/distance for transparency — same "hand-curated real-world fact beats geometry" precedent the entry-point list itself already sets, not a general escape hatch. **13 of the 19 flagged sites independently corroborated this pass** (BioRock Reef Project, Perry Street, Delray Wreck, Reefline "Traffic Jam", Ocean Inlet Park cluster, Blue Heron Bridge/Phil Foster area, South Beach shore-diving character) — real, named sources describing genuine shore/snorkel access, not just geometry. The 6 reef-band-derived sites (Datura/El Prado/Vistamar 1st/2nd) have no independent web presence under their app-generated names to check — their evidence is the founder's own dive + FWC Unified Reef Map, already the module's baseline, not a gap. **Blast radius confirmed at every step**: re-ran the full 445-site audit after each fix — Phil Foster: exactly 3 changes; Hollywood: 0; exceptions: exactly the 2 targeted sites, nothing swept in. **6 new regression tests** (2 entry corrections, 2 exception overrides, 1 exception-does-not-oversuppress-a-real-neighbor guard). `tsc --noEmit`, lint, **596 passing**, `npm run build` all clean; all live writes applied and verified.
- [x] `T21.32` **Shore-entry line on the site map — a dashed line from the shore to the site, for shore-accessible sites only** (`src/components/site-location-map.tsx` + its test). Founder: "I want to know the distance from the shore line to a dive site... a hash line from the shore to the site on the map." The distance already existed as prose (`site-dive-profile.tsx`); this is the visual complement, and it's the first thing this map draws besides the single site pin. **Recomputes shore access from the site's own coordinates via `classifyShoreAccess()`** rather than trusting a stored column or accepting a new prop — same reasoning `site-dive-profile.tsx` already established for doing this itself, and it guarantees the map and the page's own shore-access section can never disagree about the same site. **Strictly gated on `isShoreAccessible`** — never drawn for `unlikely` sites, where `nearestEntry` is still populated (per `shore-access.ts`'s "under-classify, don't guess" rule) but can be miles away; rendering that as a "swim from here" line would be actively misleading. Live-verified both directions: a real `marginal` site (Second Reef off Datura Avenue) draws the line, entry marker, and an "820 yd" label; a real `unlikely` site (NOVA Reef Roc) shows only the plain pin, unchanged from before this task. **The geometry (line, entry marker, midpoint label, camera bounds) is a pure function, `buildShoreEntryLine(entry, site)`** — extracted specifically so it's unit-testable without the real WebGL context this component's Mapbox rendering needs and this test environment doesn't have (same limitation this file's test suite already documented for the pin itself). Computes bounds as a genuine min/max envelope (regression-tested against entry/site in either geographic order), not a positional `[entry, site]` read. **Fixed a real framing bug this feature would otherwise have shipped with:** the map's `initialViewState` is site-centered at a fixed zoom, read once at mount — for a `marginal`-tier site up to `SHORE_DIVE_MAX_MILES` (0.5 mi) from its entry, that alone could crop the line off-screen. Added a one-shot `map.fitBounds()` call (gated on both `isMapLoaded` and a `hasFitEntryLineRef` guard, mirroring `site-map.tsx`'s `hasPositionedRef` pattern) that reframes the camera to include both points, with pixel-space `padding` so it stays sensible across different zoom levels — verified live, both the entry dot and the site pin land cleanly in frame. **Distance label reuses `formatShoreDistance`** (imported from `site-dive-profile.tsx`, not reimplemented) so the number on the map and the number in the prose section can never drift into disagreement. **Accessibility**: a new sr-only paragraph, `shoreEntryLineDescription()`, names the entry and the distance for a symbol/line layer that (like the pin) has no DOM node of its own — phrased with the same hedge every other shore-access surface in this app uses ("a plausible shore-entry swim… not a confirmed or recommended route"), never a stronger claim than the line itself is allowed to draw. Entry marker is a plain small circle, deliberately not routed through `pin-icons.ts` — that icon language is reserved for actual dive sites, and a shore entry is a different kind of thing that should read as visually distinct, not as a second, lesser pin. **13 new tests** (line coordinate order, entry-marker placement, midpoint placement, bounds-contains-both-points, bounds correctness regardless of point ordering, and `shoreEntryLineDescription`'s wording/unit-switching/no-overclaiming coverage) — all 8 pre-existing tests in this file unchanged and still passing. `tsc --noEmit`, lint, **587 passing**, `npm run build` all clean.
- [x] `T21.31` **Shore-access filter — the app's own core premise had no filter for it** (`src/lib/sites/explorer-preferences.ts`, `src/components/dive-site-explorer.tsx`, `src/components/nearby-dive-sites-list.tsx` + tests). Founder: "we have our shore dive constraints, why can't we have a shore dive filter?" — a real, previously-unnoticed gap: `shore-access.ts`'s classification has existed since T21.20 and is rendered per-site (detail page, pin sheet), but nothing let a diver *filter* by it, in an app whose entire premise is "can I walk in from shore." Live counts at the time this landed: **27 of 445 sites are shore-accessible** (21 `likely` + 6 `marginal`) — the other 418 are `unlikely`. Also closes a real gap between instruction and delivery: the founder explicitly asked for this earlier in the session ("we just use our default filter of shore dive") and it was never built — type and difficulty filters were, this wasn't. **Deliberately three grouped options, not four raw enum values** — `ShoreAccessFilter = "all" | "accessible" | "boat"`, with `likely`+`marginal` collapsed into one "Shore accessible" chip rather than mirroring the type/difficulty filters' one-chip-per-enum-value pattern. Reasoning: a diver filtering for shore dives wants *both* an easy walk and a long-swim walk in one click, and these chips are single-select, not checkboxes — mirroring the raw three-value enum would force two separate, mutually-exclusive selections to get what should be one. The finer likely-vs-marginal distinction stays visible per-site. Wired at every layer the existing filters already establish: persisted in `explorer-preferences.ts` (new `shoreAccessFilter` field, validated on read, corrupt/unrecognized values fall back to `"all"`), applied at the shared `DiveSiteExplorer` level so map pins and the list never diverge (same reasoning as every prior filter — a filter that only touched the list would leave stale pins on the map), folded into `describeActiveFilters`'s combined empty-state wording (shore-access reads first: "No shore-accessible beginner reef sites…", correct English adjective ordering) and into `computeMapEmptyStateMessage`'s `noFilterActive` check (a shore-access-only filter must not be misread as "no filter active" and blamed on distance/OpenStreetMap instead). **A `null`-shore_access site (not yet classified) matches only `"all"`**, same discipline as the difficulty filter's `level: null` — never silently swept into "boat access" for lack of data. **Also added a per-row `ShoreAccessBadge`** ("Shore"/"Boat", cyan/indigo — colors deliberately distinct from every other badge already in that row: violet=community, emerald/amber=legal-access, teal/blue/orange/red=difficulty) so a diver scanning the unfiltered list can see access at a glance without clicking in; hidden entirely (not a placeholder) for unclassified sites. Tooltips on both the filter chips and the badge repeat `shore-access.ts`'s own hedged language ("plausibly reachable… verify conditions", "most likely a boat/charter dive") rather than a confident rewrite. **New tests**: `siteShoreAccessCategory`'s three-way grouping (including the null case), `computeMapEmptyStateMessage`'s shore-access branches (label wording, combined-filter ordering, the `noFilterActive` regression guard), `explorer-preferences.test.ts`'s round-trip/corrupt-value coverage extended to the new field, and — since `nearby-dive-sites-list.tsx` just got a real fix for filter buttons being remounted mid-interaction (T21.30) — the DOM-node-identity regression test was extended to cover the new shore-access chip too, not just the original two rows. **Verified live**: clicking "Shore accessible" against the real database produced exactly 27 results (matching the live count precisely) in both the list and the map's rendered pins, screenshot-confirmed. `tsc --noEmit`, lint, **579 passing**, `npm run build` all clean.
- [x] `T21.30` **Filter buttons went unresponsive after repeated clicking — root cause was two structurally-different render trees for the same controls, not a filter-logic bug** (`src/components/nearby-dive-sites-list.tsx`, new `nearby-dive-sites-list.test.tsx`). Founder-reported (2026-08-10): "first 2 clicks seem fine but then you keep on click and then none work, even the original working ones stop working." **Live-caught, not just theorized**: with a genuinely-resolved geolocation override (`agent-browser set geo`, which took real effort to get working reliably in this harness — see below), a rapid filter-click pass caught the exact failure: `document.querySelectorAll('button')` transiently returned **zero** matches for a filter chip, and a click on "Unclassified" landed as `siteTypeFilter: "shore_reef"` in the committed state — not a no-op, an actively wrong value. **Root cause**: `NearbyDiveSitesList` rendered `SiteTypeFilterRow`/`DifficultyFilterRow` from **two separate `return` statements** — one for `!coords`, one for `coords` resolved — each a structurally different JSX tree. React can only update a component in place across renders that return the *same-shaped* tree at the same position; two different `return`s are two different trees, so the instant `coords` resolved (asynchronously, via `useGeolocation`'s effect — can land at any point, including mid-click), React unmounted the entire `!coords` tree, filter buttons included, and mounted the `coords`-resolved tree fresh. A click landing in that transition window hit a button already removed from the DOM, or fired during the handoff against inconsistent state. **Fix: collapsed both branches into one stable tree** — a single `return`, with only content (header text, the optional radius `<select>`, results vs. empty-state message) varying on `hasCoords`, so the filter row's position in the tree never changes and React updates it in place instead of tearing it down. **8 new tests**, this component's first ever (it had zero coverage): the regression test that would have caught the original bug captures the literal filter-button DOM node before a `coords: null → resolved` transition and asserts `toBe()` the same node afterward (real reference equality, not a lookalike) — both directions (resolve and un-resolve), plus filter selection surviving the transition, header/control visibility, and empty-state copy for both states. **Verified live twice**: a 19-click sequence with 150ms gaps (matching the original catch), and a tighter 20-click stress pass at 30ms gaps fired immediately after a fresh reload (deliberately targeting the narrow coords-resolution window) — zero buttons missing, zero console errors, correct final committed state, both times. **Investigation note, since it consumed real effort**: forcing real geolocation to resolve in this headless test harness required discovering that `agent-browser` runs a persistent daemon that silently ignores relaunch flags (including `--headed`) unless explicitly closed first (`agent-browser close`) — every earlier attempt this session that appeared to fail (a `getCurrentPosition` monkey-patch via `--init-script`, `set geo` without closing first) was likely reusing a stale daemon/session rather than genuinely failing to grant permission. Once closed-and-reopened properly, `set geo` worked reliably. Also confirmed `agent-browser errors --json` can return byte-identical stale content across a full dev-server restart — it was not trustworthy evidence on its own this session; live DOM/state checks (button queries, `localStorage`, screenshots) were the real signal throughout. `tsc --noEmit`, lint, **567 passing**, `npm run build` all clean.
- [x] `T21.28` **Reef-band import completed and written live — 14 real `shore_reef` sites, using measured NOAA depth over the literature fallback** (`src/lib/sites/reef-band-sites.ts` updated, live write via a scratch runner). Picked up directly from T21.25's dry-run-only pass once T21.24's bathymetry split landed. **`resolveDepthFt` now prefers `band.depthFt`** (real NOAA-measured range, populated by `deriveReefBandsWithDepth`) **over the Datura literature citation** — a genuine sounding beats a citation, and the preference now applies to every entry, not just Datura's exactly-two-band case. `buildDescription`'s depth line was wrong under the old assumption ("not measured by this derivation itself") for any non-null depth; it now names its actual source (`measured`/`literature`/`none`) so the disclosure text never claims a real NOAA reading wasn't measured, or vice versa. **Depth values rounded to 1 decimal** before being written into description text — NOAA's raw float (`9.550075512295319 ft`) implied false survey-grade precision; the DB column would've rounded on insert regardless, but the plain-text description never would have. **A second real filter applied before writing:** the depth-aware banding split Datura into 5 bands, not the expected 2 — bands 1-2 matched the founder-confirmed first/second reef split exactly (9.6-19.3 ft / 20.4-41.1 ft), but 3 more appeared further offshore, several as **single-sample bands** (`nearestYards === farthestYards`, only reachable with exactly one transect sample). A "reef line" that can't be described as spanning any distance isn't a real feature — excluded 4 such fragments (out of 18 total candidates) as depth-jump-heuristic noise in deeper, less-charted water, not real reef lines; the multi-sample bands at the same distances were kept. **5 new tests** in `reef-band-sites.test.ts` covering the measured-vs-literature preference (including the real regression case: Datura's own two-band split now has bathymetry coverage, so measured 9.6-19.3/20.4-41.1 ft must win over the pinned 12-20/30-50 ft citation), a single-non-null-bound depth still counting as measured, and the "no bathymetry and no literature" honest-null path. **Live result: 14 sites written** (Datura ×2, El Prado ×3, Red Reef Park ×2, South Beach ×2, Vistamar ×3 — Phil Foster/Mizell-Johnson/Hollywood North Beach stayed at 0 bands, no reef habitat in range), 0 flagged as duplicates against the existing 431. `sites` now **445 total, 14 tagged `shore_reef`** — previously 0, meaning the "Reef" type filter had been permanently empty. `tsc --noEmit`, lint, **549 passing**, `npm run build` all clean before the write; re-verified clean after.
- [x] `T21.29` **Map went silently blank right after load — fixed by giving the map the same empty-state honesty the list already had** (`src/components/dive-site-explorer.tsx`, `src/components/nearby-dive-sites-list.tsx`, `src/components/site-map.tsx` + new `dive-site-explorer.test.ts`). Founder-reported (2026-08-10): reload the homepage, pins render, then a "post-load flicker" and every pin disappears. Root cause, confirmed by reading `dive-site-explorer.tsx`'s existing `mapSiteMarkers` ternary: before geolocation resolves, the map shows the full fallback `filteredSites`; the instant real geolocation resolves, it swaps to `filteredInRadius` — and if the resolved location has zero sites within the selected radius (default 25 mi), that's an empty array, with **zero on-map explanation**. `NearbyDiveSitesList` already had honest copy for exactly this case ("No sites within 25 mi of your location yet"); the map had no equivalent, so a legitimate "you have no nearby sites" result and an actual failure were visually identical — a map that goes blank. **Live repro attempts genuinely failed** despite real effort (a `getCurrentPosition` monkey-patch via `--init-script`, then `agent-browser`'s native `set geo` command in two call orderings) — headless Chrome's geolocation permission never actually resolved in this harness, so `coords` stayed `null` throughout and the bug's own trigger condition was never reached live. Documented as an honest tooling limitation, not treated as disproving the diagnosis: the code-level cause is unambiguous and the symptom (all-then-abruptly-none, not partial) only fits this one all-or-nothing data-source swap. **Fix: `SiteMap` gained an `emptyStateMessage?: string | null` prop**, rendered as a small `role="status"` overlay card when supplied and `siteMarkers.length === 0` — same visual language as the existing pin-preview-sheet overlay. `DiveSiteExplorer` computes the message via a new exported pure function, `computeMapEmptyStateMessage()`, extracted specifically so this four-branch decision tree (no filter/no external search, no filter/external search ran, a filter is active, radius already at max so "try expanding" doesn't apply) is unit-testable without mounting the component (`SiteMap`/`useGeolocation`/the search-nearby `fetch` would all need mocking to render `DiveSiteExplorer` at all). Reuses the exact wording `NearbyDiveSitesList` already uses via two helpers (`radiusLabel`, `describeActiveFilters`) **moved** (not duplicated) from `nearby-dive-sites-list.tsx` into `dive-site-explorer.tsx` — moved rather than re-exported the other way because the list already imports `siteDifficultyLevel` from the explorer for the identical "one shared source of truth" reason, and importing back would have been a circular import. **Two real bugs, both caught by the new tests, neither by review:** (1) `MAX_RADIUS_MILES` was originally `RADIUS_OPTIONS_MILES[RADIUS_OPTIONS_MILES.length - 1]` — but that array's own last element is `Infinity` (the "All" option), so `radiusMiles < MAX_RADIUS_MILES` was true for *every* finite radius, meaning "you're at the max, stop suggesting expansion" could never fire for the real largest numbered option (250 mi); fixed to `Math.max(...RADIUS_OPTIONS_MILES.filter(Number.isFinite))`. (2) The same `RADIUS_OPTIONS_MILES` constant was declared *after* a new module-level constant that referenced it — a temporal-dead-zone `ReferenceError` waiting to happen at module load, caught by re-running `tsc`/tests before it ever shipped; the declaration was moved above its first use. **10 new tests** in `dive-site-explorer.test.ts` covering all four message branches, the max-radius suffix cutoff (the exact case that caught bug #1), the OpenStreetMap-credit wording only appearing when an external search actually ran (never when a client-side filter is why results are empty, which would misattribute the cause), and the three "nothing to explain" early-return cases (has results / coords not resolved / radius is "All"). Live-verified the **false-positive** direction directly, since the true-positive path couldn't be forced: reloaded with a real Florida viewport and all filters at "all", confirmed the map renders its normal full pin set with no spurious overlay. `tsc --noEmit`, lint, **559 passing**, `npm run build` all clean.

## Phase 2: Community Data & Automated Monitoring

### Task 14: Post-Dive Micro-Prompt Engine (→ Task 14)
- [x] `T14.1` Define "active dive-plan check-in state" trigger logic (not bare GPS proximity) — `src/hooks/use-post-dive-prompt-trigger.ts`. Pure, unit-testable `evaluatePostDiveTrigger()` (real test coverage, `use-post-dive-prompt-trigger.test.ts`) fires on two edges: a stubbed `DivePlanCheckInState` transitioning active→inactive (still stubbed — real `dive_plans` model exists as of `T11.5` but this edge isn't wired to it yet, separate follow-up), or the Safe-Return timer's `running → checked-in` transition. Deliberately does NOT fire on `expired`. No GPS, no polling. **Re-upgraded to `[x]` 2026-08-08:** the `running → checked-in` edge is now genuinely wired into the real flow (see `T14.2`) — verified live via `agent-browser` (started a real timer, checked in, confirmed the prompt UI's manual escape hatch renders correctly in the actual `/safe-return` page, not just the `/post-dive` demo).
- [x] `T14.2` Single-tap condition-logging prompt UI — `src/components/post-dive-prompt/` (visibility/current/marine-life chips, one-tap dismiss, localStorage persistence with a TODO for the pending Supabase migration). **Wired into the real Safe-Return check-in flow 2026-08-08:** `safe-return-timer.tsx` now calls `usePostDivePromptTrigger({ safeReturnStatus: timer.status })` at the top level (the hook tracks its own previous-status internally via a `useRef`, so no extra tracking was needed at the call site); `checked-in-view.tsx` renders the real `PostDivePrompt` when the trigger fires, or an always-available "Log dive conditions" manual link (`showManually()`, per `T14.3`) when it hasn't — both sit above the existing calm, non-celebratory check-in confirmation, no new color/motion added. Verified independently: `tsc`/lint/105-test-suite/build all clean, and a live `agent-browser` check confirmed the manual path renders the real condition-logging form correctly inside the actual check-in screen. `siteId` is `null` for now (no real dive-plan/site link wired to this specific hook call yet — `T11.5`'s new `dive_plans` table exists but isn't threaded through here).
- [x] `T14.3` Settings toggle to disable inference-based triggers — `src/hooks/use-post-dive-prompt-settings.ts` (same `useSyncExternalStore` localStorage pattern as the Safe-Return timer); a `showManually()` escape hatch always works regardless of the toggle. Demo/test harness at `/post-dive` (not linked from the homepage).

### Task 15: Safe-Return Emergency Timer (→ Task 15, → §0.1 resolution — life-safety-critical, extra rigor)
- [x] `T15.1` Countdown timer UI (start/duration selection) — `src/app/safe-return/page.tsx` + `src/components/safe-return/`
- [x] `T15.2` Notification API integration for expiry alert — `src/lib/safe-return/alert-channel.ts` (`LocalAlarmChannel`)
- [x] `T15.3` Web Audio alarm sound — same file, two-tone siren pattern, loops until `stop()`
- [x] `T15.4` Vibration API integration where supported — same file, guarded (`navigator.vibrate` checked before use, absent on iOS Safari by design)
- [x] `T15.5` Explicit-confirmation check-in (not a passive dismiss)
- [x] `T15.6` Onboarding disclaimer screen — states plainly it only alerts on this device, doesn't notify anyone else, and is less reliable when backgrounded/locked, especially on iPhone
- [x] `T15.7` Persist timer state across reloads — `src/hooks/use-safe-return-timer.ts`, localStorage-backed, modeled as a `useSyncExternalStore` external store (not an effect+setState hydration flag — avoids both a lint violation and a latent hydration-mismatch bug from a plain `useState` initializer reading real client capability against an SSR-safe-but-different server value)
- [ ] `T15.8` Manually test on iOS Safari (installed-PWA and not) — document actual observed reliability, feed back into disclaimer copy if it's worse than assumed — **still needs a physical device, founder action**
- [x] `T15.9` Abstract the alert-channel interface so a future Web Push/SMS channel can be added without a rewrite — `AlertChannel` interface in `alert-channel.ts`, `LocalAlarmChannel` is v1's only implementation

**Note:** the agent building this feature hit two dropped connections mid-session; the final two lint errors (`react-hooks/set-state-in-effect` in the timer hook and the status panel) were finished by hand rather than via a third agent retry — both fixed with the same `useSyncExternalStore` approach the agent had already correctly identified before it was interrupted. Full project build/lint verified clean afterward with all four workstreams merged together.

### Task 16: Local Dive Shop (LDS) Logistics Layer (→ Task 16)
- [x] `T16.1` Map integration showing fill-station pins from `lds_status` (P0-B.3) — `src/components/site-map.tsx` now accepts `ldsMarkers` (defaults to mock data), status-colored dots + popup with `ProvenanceBadge`. Data model: `src/components/lds/lds-status.ts`, correctly treats `lds_status` as an append-only log (`latestStatusPerShop()` collapses history client-side, matching the migration's design) rather than assuming pre-collapsed rows.
- [x] `T16.2` "Last verified" timestamp UI (reuse T13.1's freshness component) — `src/components/lds/last-verified-badge.tsx`, thin wrapper reusing `FreshnessBadge` unmodified (not forked)
- [x] `T16.3` Community submission form for LDS status updates (routes through P0-B.4's moderated write path) — `src/components/lds/lds-submission-form.tsx`; no `VERIFIED` option in the form at all, every submission hardcoded `COMMUNITY` (matches the RLS `with check`). `created_by` is `null` for now — **TODO(P0-A): wire the real auth session in once P0-A.4's sign-in is connected here** (P0-A.4 is now live — this TODO is unblocked, re-audit per the new process rule). **Also flagged 2026-08-08 by the dive-shop-perspective review:** there is no path, even manual/founder-run, for a shop to get its own listing to `VERIFIED` — see `plan.md` v5's Task 16 additions (value exchange, VERIFIED claim process, shop-facing return data, explicit scope boundary, outreach plan) before real shop outreach begins.

## Phase 3: Visual & Webcam Intelligence

**Sourcing model applies to all of Tasks 17–19 (→ Phase 3 sourcing model, → §9): allowlist/partnership only, never scraping.**

### Task 17: Live Camera API / Stream Ingestion (→ Task 17)
- [x] `T17.1` `camera_sources` allowlist schema — `supabase/migrations/0003_camera_sources.sql`. `pending_review`/`approved`/`rejected` status is the access gate; public read limited to `approved` only (candidates from the not-yet-built Task 19 discovery agent stay invisible until a human approves — the actual moderation-queue enforcement, not just a UI convention). Approve/reject requires the service-role key — no moderator role modeled yet, same documented gap as `0002_rls.sql`.
- [~] `T17.2` Ingestion connector for the first personally-negotiated camera source(s) — **partially done 2026-08-08.** The founder personally named real starting sources (YouTube-hosted beach cams from `visitlauderdale.com`'s official live-cam page, plus the Coral City Camera underwater reef project) directly in-session — this is the human-review step `THREAT_MODEL.md` §9 requires, given directly rather than via the moderation queue UI, and is explicitly *not* Task 19's discovery pipeline (still correctly inert). 4 real `camera_sources` rows inserted as `status = 'approved'`, `reviewed_by` set to the founder's real profile id, `reviewed_at` set to the real insertion time — reads identically to a real moderation decision if audited later. Each source's embeddability was verified directly (`curl -I` for `X-Frame-Options`) before insertion, not assumed — YouTube's regular watch/live page URLs block framing, only `/embed/<id>` doesn't, so URLs were corrected to that form. `EMBED_DOMAIN_ALLOWLIST` (`T20.2`) updated to match (`www.youtube.com`, `www.coralcitycamera.com`). Full traceability record: `supabase/sources/webcam-sources.md`. **Not yet done:** an actual ingestion *connector* (a real ETL/monitoring process) — this is allowlist population, not a connector; `~12` more candidate cams from `visitlauderdale.com` remain unverified/uninserted (documented as a next step, not silently dropped).
- [x] `T17.3` Confirm stream embedding/bandwidth stays within free-tier hosting limits — confirmed by design, not just measurement: `src/components/media-embed/media-embed.tsx`'s live-stream (`<video src={current.url}>`), embedded-player (`<iframe src={current.url}>`), and refreshing-stills (`<img src={stillSrc}>`) tiers all point directly at the external source URL client-side — none are proxied through a Vercel route. Stream/image bytes flow straight from the source to the browser, so Vercel's bandwidth quota is only touched by the app's own JS/HTML, regardless of how much video a user watches. Re-confirm once `T17.2` wires up a real source and real-world usage is observable.

### Task 18: Visual Telemetry Extraction Pipeline (→ Task 18, → §9)
- [x] `T18.1` **Decision:** `claude-haiku-4-5-20251001` (cheapest current vision-capable Claude model), per CLAUDE.md's "default to the latest Claude models for AI-backed workers." **Flagged, not silently resolved:** the Anthropic API has no free tier at all, in real tension with the no-paid-dependency-without-flagging standard — `src/lib/webcam-extraction/model.ts`'s header comment lays this out at length. Resolved by gating all real usage behind `WEBCAM_EXTRACTION_ENABLED` (must be the literal string `"true"`, defaults off) plus `T18.5`'s hard daily cap. **Turning it on against real spend is an explicit founder-confirmation item** (`supabase/README.md` open item 10) — nothing in this codebase does that on its own.
- [x] `T18.2` Scheduled worker to snapshot + call the vision API — `src/app/api/cron/webcam-extraction/route.ts` (`GET` for Vercel Cron, `POST` for manual/dev trigger, both gated on `CRON_SECRET`, fails closed if unset). Designed to be safe at Vercel Hobby's once-a-day cron limit: the rate cap is a daily budget tracked by counting today's `webcam_readings` rows, not a per-invocation budget. No `vercel.json` cron schedule added yet — deliberately, since that would presume `T18.1`'s cost decision is already confirmed.
- [x] `T18.3` Store `MODEL_INFERRED` results with confidence score + timestamp — `supabase/migrations/0005_webcam_readings.sql` (`webcam_readings` table: nullable `visibility_meters`/`chop_state`, mandatory `confidence`, `model_id`, `captured_at` + `created_at`). Public read (a row only exists because its source camera was already `approved`), no anon/authenticated write — service-role-only via `src/lib/supabase/admin.ts`, same documented gap as `camera_sources` approve/reject.
- [x] `T18.4` Distinct UI styling for inferred vs. verified data (reuses P0-B.5 pattern) — `src/components/webcam-readings/reading-badge.tsx`, wraps the existing `ProvenanceBadge`'s `MODEL_INFERRED` treatment rather than reinventing it.
- [x] `T18.5` Hard volume/rate cap on API calls — `src/lib/webcam-extraction/rate-cap.ts`, `DAILY_CALL_CAP = 20` (env-overridable via `WEBCAM_EXTRACTION_DAILY_CALL_CAP`, invalid overrides fall back to the safe default, never "unlimited"). Real, enforced, fail-closed check (an uncountable/errored count is treated as over-cap, not under) — not just a documented intention. **Honest split:** the rate-cap logic, result shaping, and DB/UI wiring are real and correct today with no live credentials needed; the actual Anthropic API call (`vision-client.ts`) and webcam-snapshot fetch (`snapshot-fetch.ts`) are real code, honestly unproven end-to-end pending a live `ANTHROPIC_API_KEY` and `T17.2`'s first real camera source.

### Task 19: Automated Webcam Discovery Agent (→ Task 19, → §9)
- [~] `T19.1` Candidate-discovery workflow (LLM-assisted, constrained to allowlist candidates only) — **scaffold only, by explicit founder decision**, per `THREAT_MODEL.md` §9's unresolved "legal review of the scraping/redistribution approach for webcam feeds" gate (not done yet). Real, working pieces: `src/lib/webcam-discovery/extract-candidate.ts` (LLM normalization via `claude-haiku-4-5-20251001`, real Anthropic API call once a key exists) and `submit-candidate.ts` (inserts into `camera_sources` as `pending_review` through the normal authenticated-user RLS path — never the service-role client — so a discovery-agent submission is exactly the same trust level as a human's). **Deliberately inert:** `discover-candidates.ts`'s default `CandidateDiscoverer` does zero network access — returns an empty, founder-curated seed list — and the manual-trigger route (`src/app/api/webcam-discovery/route.ts`, POST-only, signed-in required, max 20/request) requires an authenticated caller to supply candidate URLs directly, with no fallback to autonomous discovery. Do not wire up real web search/crawling here without first confirming the legal-review gate has actually cleared.
- [x] `T19.2` Human-review moderation queue UI — `src/app/moderation/camera-sources/` (server component does the privileged read via the service-role client since RLS only lets a user see their own submissions; client component handles Approve/Reject). Demo/test route, not linked from the homepage (same convention as `/media-demo`, `/post-dive`).
- [x] `T19.3` Approval flow to promote a candidate into the allowlist — never auto-published — `src/app/api/camera-sources/[id]/review/route.ts`, service-role update scoped to `status = 'pending_review'` (race-safe against double-review, returns 409 if already decided). **Honest security gap, stated plainly in the route's header comment, the page's on-page notice, and `supabase/README.md` item 6:** this is gated on "caller has a valid authenticated Supabase session," not "caller is a moderator" — no moderator/role column exists anywhere in this schema yet, so any signed-in user can approve/reject today. Deliberately not papered over with a fake role system; real fix is future work once a role model exists.

### Task 20: Unified Media Embed & Fallback UI (→ Task 20)
- [x] `T20.1` Media component with fallback order: live stream → embedded player → refreshing stills — `src/components/media-embed/`. Explicit failure detection at every tier (video `onError`/`onStalled`, 8s iframe-load timeout since cross-origin iframes don't reliably fire `onerror`, image `onError`) advances to the next tier; terminal `unavailable` state has a Retry button. Demo at `/media-demo` (not linked from homepage), including a deliberately non-allowlisted source to prove the refusal path.
- [x] `T20.2` Sandbox third-party embeds via iframe `sandbox` attribute + domain allowlist — `sandbox="allow-scripts"` only, `allow-same-origin` deliberately omitted (the classic risky pairing; forces an opaque origin so the embed can't use its own cookies either) — may break some real-world embeds that rely on their own cookies, accepted as the safer default, revisit per-domain if a real partner's embed breaks it. `EMBED_DOMAIN_ALLOWLIST` checked synchronously in the render path so a disallowed hostname's iframe is never mounted, not just caught after the fact. **Decided, not deferred:** stays static/hand-curated rather than derived from `camera_sources` — deriving it would make the render-time security check asynchronous (regressing the "never mount, not even transiently" property), and `camera_sources.status = 'approved'` answers a different trust question ("is this a real camera") than "is this hostname safe to iframe." The gap where the two lists could drift apart is closed by detection, not automation: `src/lib/camera-sources/embed-allowlist-consistency.ts` + `src/app/api/admin/embed-allowlist-consistency/route.ts` (`CRON_SECRET`-gated) flags any approved, embeddable-shaped `camera_sources.source_url` whose hostname isn't allowlisted, for a human to resolve.
- [x] `T20.3` Stream-down fallback handling — covered by `T20.1`'s per-tier failure detection above (not a separate mechanism)

**Note:** the Post-Dive Micro-Prompt agent hit a stalled-connection failure near the very end of its run (its last visible message was mid-fix on an unrelated import). Rather than a third resume/retry, I checked the files it left behind directly — `tsc --noEmit`, `npm run lint`, and a full `npm run build` all passed clean on the first try, so its work was actually complete; it just didn't get to send its closing report. Full project build verified clean afterward with all four of this round's workstreams merged (8 routes total, up from 5).

**Note:** `T12.1`, `T18.1`–`T18.5`, and `T19.2`/`T19.3` were built as three parallel agents in isolated git worktrees, then reviewed and merged by hand rather than trusted blind. Two real conflicts surfaced from the agents working without visibility into each other: (1) both the `T12.1` and `T18.3` agents independently claimed migration number `0004` — resolved by renumbering `T18.3`'s to `0005_webcam_readings.sql`; (2) both the `T18` and `T19` agents independently built a near-identical service-role Supabase client (`src/lib/supabase/service-role.ts` vs. `admin.ts`) — resolved by keeping `T19`'s `admin.ts` (merged first) and repointing `T18`'s `run-extraction.ts` at it, deleting the duplicate. Also corrected `T18.1`'s model ID from the agent's `claude-haiku-4-5` to the actual current ID `claude-haiku-4-5-20251001`. `supabase/README.md` was hand-merged (each agent's worktree only saw its own additions, not the others') rather than copied wholesale from any one agent. `tsc --noEmit`, `npm run lint`, and a full `npm run build` all verified clean after every merge step, with all three workstreams combined (13 routes total, up from 8).

**Note:** a second round fanned out `P0-B.4` (rate limiting), `T20.2` (embed allowlist decision), and `T19.1` (discovery-agent scaffold) as three more parallel worktree agents; `T17.3` and `P0-C.3` were resolved directly (verification-only, no code needed — see their entries above). `T19.1` was scoped narrowly first: `THREAT_MODEL.md` §9 gates Tasks 17–19 on a legal review that hasn't happened, so before fanning it out, the founder was asked how to handle it and chose "scaffold only, no live web discovery" over building it fully or skipping it — the agent held that boundary correctly (verified by reading `discover-candidates.ts` directly: zero network calls, an empty seed list, and the manual-trigger route requires an authenticated caller to supply candidates directly with no autonomous fallback). No cross-agent conflicts this round (each touched disjoint files, and `T19.1` correctly reused the already-existing `admin.ts`/`server.ts` clients rather than duplicating them again). `README.md` sections from both `P0-B.4` and `T20.2`'s worktrees had to be hand-merged against each other and against the already-merged `T20.2` content, since neither agent's worktree saw the other's or the prior round's additions. `tsc --noEmit`, `npm run lint`, and a full `npm run build` verified clean after every merge step (16 routes total, up from 13).
